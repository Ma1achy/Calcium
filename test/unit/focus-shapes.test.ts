// C09 I105 and I106 — §018's focus shapes: a choice, and a continuous control.
//
// **Spec-first**: the rows are declared here and land with the code. Both are
// classification tables rather than traces — the cells differ by which of two
// channels moved, and nothing happens between them a sequence could key on.
import { describe, it } from "vitest";

describe("C09 I105 — a choice is one shape, and its two channels never substitute", () => {
  it.todo("T1.72 (C09 I105, §018): four cells — the mark carries chosen, the wash carries focus, and the label is part of the shape — not deferred on a component: the row lands with C09 commitment 86 in the next commit");
});

describe("C09 I106 — a continuous control has three states and the value is none of them", () => {
  it.todo("T1.73 (C09 I106, §018): the wash reaches label, track and value; inside is weight and a painted handle; the value never moves — not deferred on a component: the row lands with C09 commitment 87 in the next commit");
});
