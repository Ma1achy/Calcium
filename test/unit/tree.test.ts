// C04 §3ap — the tree (§105): the twisty is content, the guides are decoration.
import { describe, it } from "vitest";

describe("C04 §3ap — tree", () => {
  it.todo("T1.52 (C04 I129): node ids, visibility and each node's own flag — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.53 (C04 I130): §105's figure, then the ladder rung by rung — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.54 (C04 I131): the twisty is the disclosure pair, never focus — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.55 (C04 I129): op expand reaches a node at any depth — not deferred on a component: specified ahead of the code in this commit");
});
