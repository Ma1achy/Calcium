/**
 * C04 I120 — every member `Plot` declares is refused a wrong value at the
 * document gate, and the members that are not are a named list with reasons.
 *
 * Spec-first: the invariant is ruled and the checks are not written, so these
 * are `it.todo` rows rather than a file that passes by asserting nothing.
 */
import { describe, it } from "vitest";

describe("C04 I120 — a wrong value per member, driven at every member Plot declares", () => {
  it.todo(
    "T1.43 (C04 I120, F1082): the table is compared to `Plot`'s declaration by equality in both directions, the member count is asserted first, and every entry is refused by validateDocument with the member named — not deferred on a component: the twenty checks land with this ruling's code commit",
  );
  it.todo(
    "T1.44 (C04 I120): the control — every member's own legitimate value is accepted on a form that carries it, so the table cannot be satisfied by a gate that refuses everything — not deferred on a component: it lands with T1.43",
  );
  it.todo(
    "T1.45 (C04 I120, F1082): the three shapes named rather than looped — `yMin: \"x\"` where plotAxisCrossErrors would have skipped it, a calendar's `startDate` refused without its `calendarUnit` sibling, and `yMin: 6, yMax: 0` refused on the relation no member rule can see — not deferred on a component: it lands with T1.43",
  );
});
