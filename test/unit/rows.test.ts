// C09 I72 — the rows arm's normaliser against the tokeniser's own serialiser.
import { describe, it } from "vitest";

describe("C09 I72 — normaliseRow", () => {
  it.todo(
    "T1.46 (C09 I72): normaliseRow equals the tokeniser's serialiser byte for byte over every corpus row at four widths under three capability sets and over ten thousand seeded rows of random text and SGR — not deferred on a component: the code commit replaces this row",
  );
});
