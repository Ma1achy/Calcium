// C04 §3aq — the split (§105): two peer panes, and the divider is a control.
import { describe, it } from "vitest";

describe("C04 §3aq — split", () => {
  it.todo("T1.56 (C04 I132): two children, a height and a divider — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.57 (C04 I133): §105's figure, and the divider is the left pane's bar — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.58 (C04 I134): each pane's elements, and the divider's accent — not deferred on a component: specified ahead of the code in this commit");
});
