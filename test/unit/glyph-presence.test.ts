// C09 §7c — every registry glyph resolves to a mark this tree can draw.
//
// Spec-first: the row arrives with the scan in the next commit. TD6 wants the
// blocker named, and it is the scan in this MR and not deferred on a component.
import { describe, it } from "vitest";

describe("C09 §7c — the carrier matrix's marks", () => {
  it.todo(
    "T2.155 (C09 I88): every current registry glyph resolves to a mark, or is named with its home and its reason — awaiting the SS65 scan in this MR's second commit, not deferred on a component",
  );
});
