// C22 §6l.10 — the label on the prompt's rule (§069, `R-COL-003`).
import { describe, it } from "vitest";

describe("C22 §6l.10 — the labelled rule", () => {
  it.todo(
    "T1.65 (C22 I111, §6l.10): the label sits inline-end on the upper rule with one trailing glyph, painted as a ground, and a frame with no label is byte-identical to the one that shipped — not deferred on a component: the slot lands in this MR",
  );
  it.todo(
    "T1.66 (C22 I111, §6l.10, R-BLK-175): the label is gone below 60 columns and whenever it would leave no rule glyph, and the frame's height does not move — not deferred on a component: the drop lands in this MR",
  );
});
