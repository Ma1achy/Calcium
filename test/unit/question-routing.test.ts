// C23 §7f — replace or float: does the answer need the prompt? (§101)
//
// **The rows are about a product of two axes, not about six consumers.** §101's
// table has *blocking* and *replacing* as independent, and a build that tied
// them satisfies four of its six cells — so what is asserted is the table, and
// the typed reply is the cell that separates the design from the tie.
import { describe, it } from "vitest";

describe("C23 §7f — replace or float", () => {
  it.todo("T1.68 (C23 I73, §7f, §101) — not deferred on a component: the routing lands in this MR's second commit: the table is total and its two axes are independent");

  it.todo("T1.69 (C23 I73, §7f) — not deferred on a component: the routing lands in this MR's second commit: choosing reply… moves one question from replacing to floating, with its id and its owner unchanged");

  it.todo("T4.70 (C23 I74, C22 I80, §7f) — not deferred on a component: the routing lands in this MR's second commit: a replacing question leaves the region's height and the transcript's rows where they were");
});
