// §047 — a stale reading says when it was taken, and its content dims while its
// chrome does not (R-HON-002; C04 I127, C09 I109, C09 I110, C10 I59).
import { describe, it } from "vitest";

describe("§047 — the stale reading", () => {
  it.todo("T1.51 (C04 I127): staleForMs validates as a non-negative finite number — not deferred on a component: the field lands in the next commit of this MR");
  it.todo("T1.74 (C09 I109): age over the unit boundaries — not deferred on a component: the formatter lands in the next commit of this MR");
  it.todo("T2.172 (C09 I109, R-HON-002): §047's figure, and the title yields before the notice — not deferred on a component: the panel's notice lands in the next commit of this MR");
  it.todo("T2.173 (C09 I110): a stale panel's content dims and its border does not — not deferred on a component: the receded subtree lands in the next commit of this MR");
  it.todo("T1.50 (C10 I59): recede resolves every palette slot as tone.dim at every depth — not deferred on a component: recede lands in the next commit of this MR");
});
