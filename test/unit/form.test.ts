// C04 §3ar — the form (§105): the label is Fixed, the field Grows, the hint is decoration.
import { describe, it } from "vitest";

describe("C04 §3ar — form", () => {
  it.todo("T1.59 (C04 I135): fields, buttons and unique ids — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.60 (C04 I136, C09 I119): §105's figure, the narrow rungs and the draft — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.61 (C04 I137): a submit writes the values as arguments — not deferred on a component: specified ahead of the code in this commit");
});
