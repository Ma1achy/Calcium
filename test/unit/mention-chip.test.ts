// C19 I28 — a candidate that accepts into a chip (§011).
//
// **Spec-first**: the row is declared here and lands with the code. §011's
// sentence — *@ pulls a file into the turn, and it is a chip like a paste* — is
// one seam between two halves that both shipped and had never met.
import { describe, it } from "vitest";

describe("C19 I28 — a candidate may accept into a chip", () => {
  it.todo("T1.71 (C19 I28, §011, C17 I25): a candidate carrying chip parts accepts into a chip, in one undo unit, with C17's label — not deferred on a component: the row lands with C19 commitment 24 in the next commit");
});
