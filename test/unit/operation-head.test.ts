// C23 I76 and C09 I104 — §036's operation surface.
//
// **Spec-first**: the rows are declared here and land with the code. §036 draws
// the head twice and they are not the same line — bracketed while it runs, flat
// once it stops — and the bar goes in all three stopped states for two arguments
// that do not substitute for each other.
import { describe, it } from "vitest";

describe("C23 I76 — an operation's head brackets while it runs and flattens when it stops", () => {
  it.todo("T1.71 (C23 I76, §036): four running shapes and four stopped ones — the aside groups, and settlement flattens — not deferred on a component: the rows land with C23 commitment 66 in the next commit");
  it.todo("T1.72 (C23 I76, §036): the bar is gone when settled, when cancelled and when failed — three cases, not one — not deferred on a component: the rows land with C23 commitment 66 in the next commit");
});

describe("C09 I104 — a meter's label column costs nothing when there is no label", () => {
  it.todo("T1.71 (C09 I104, §036): an empty label spends no cells, and the bar is wider by exactly the column — not deferred on a component: the row lands with C09 commitment 85 in the next commit");
});
