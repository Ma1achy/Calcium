// C22 §6m — the linear rendering's events and bodies (§107, ruling 29).
import { describe, it } from "vitest";

describe("C22 §6m — linear events", () => {
  it.todo("T1.74 (C22 I120, C22 I121, C22 I124): §6m.2's table, row by row, through the event function alone — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.75 (C22 I121): every kind of ONE_PER_KIND through the body function — not deferred on a component: specified ahead of the code in this commit");
});
