// C09 §7b — `R-SEL-002`'s gutter clause, over the whole registry.
//
// Spec-first: the row arrives with the check in the next commit. TD6 wants the
// blocker named, and it is the check in this MR and not deferred on a component.
import { describe, it } from "vitest";

describe("C09 §7b — surviving a naive drag", () => {
  it.todo(
    "T2.154 (C09 I87): nothing is drawn left of a block's first column, on any row, over the whole corpus — awaiting the corpus check in this MR's second commit, not deferred on a component",
  );
});
