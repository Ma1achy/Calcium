// C12 I120, §6p — a cell of clearance, or the label is dropped; a refusal reserves nothing.
// Spec-first placeholder: the ruling lands in this round's spec commit and the rows with the code.
import { describe, it } from "vitest";

describe("LC1 (C12 I120): the composed row no longer reads one number that is neither value", () => {
  it.todo("LC1 (C12 I120): a cell of clearance, or the label is dropped — not deferred on a component: the rows land with C12 I120's implementation in this round's code commit");
});
