// C09 I113, I108 — a row that sheds says so with `+n`, and is a target whose
// peek holds what it withheld (§104, parked 19).
import { describe, it } from "vitest";

describe("C09 I113 — a shed row is a target", () => {
  it.todo("T1.80 (C09 I108, I113): the shed mark is `+n` and the same string at both rungs — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.81 (C09 I113, C26 §5): a shedding kind publishes one row element per item whose detail is exactly the withheld parts, and none when nothing sheds — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T1.82 (C09 I113, C02 I9): the elements never under-list at the wide convention — not deferred on a component: specified ahead of the code in this commit");
});
