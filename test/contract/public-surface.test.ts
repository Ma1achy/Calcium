// C24 T2.21 (I34) — a consumer does not index a published type to recover a name.
// Spec-first placeholder: the ruling lands in this round's spec commit and the rows with the code.
import { describe, it } from "vitest";

describe("C24 T2.21 (I34) — a consumer does not index for a name src/ declares", () => {
  it.todo("T2.21 (C24 I34): a type a consumer has to index for belongs on the surface — not deferred on a component: the rows land with C24 I34's implementation in this round's code commit");
});
