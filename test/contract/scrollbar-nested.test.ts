// C09 I115 — nested scrollables each draw their own bar (parked 22).
import { describe, it } from "vitest";

describe("C09 I115 — two scrollables, two bars, two columns", () => {
  it.todo("T1.83 (C09 I115, R-SEL-012): an inner and an outer scroll, both overflowing, draw bars in columns 28 and 29 — not deferred on a component: specified ahead of the code in this commit");
});
