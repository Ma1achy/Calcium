// C09 §7h — the semantic node (§107, R-ACC-001, parked 29).
import { describe, it } from "vitest";

describe("C09 §7h — every block, every element", () => {
  it.todo("T2.177 (C09 I116): a role for every known kind — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T2.178 (C09 I117): id, role, name and value from the block — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T2.179 (C09 I118): children are children's nodes or elements' — not deferred on a component: specified ahead of the code in this commit");
});
