// C09 I114 — `↺` is registered (parked 17).
import { describe, it } from "vitest";

describe("C09 I114 — the revert affordance", () => {
  it.todo("T2.176 (C09 I114, SS64, SS65): `glyphs(caps).revert` is ↺ / < and the registry record agrees — not deferred on a component: specified ahead of the code in this commit");
});
