// C22 §6n — notifications, bytes in through stdin (ruling 27, `R-NTF-001`).
import { describe, it } from "vitest";

describe("C22 §6n — a session that reaches a reader who left", () => {
  it.todo("T4.105 (C22 I126, C22 I127, C22 I128, C22 I129, C01 I23, C01 I24, C16 I61, C02 I16, C02 I17): the rungs, the focus gate and the title stack — not deferred on a component: specified ahead of the code in this commit");
});
