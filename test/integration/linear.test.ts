// C22 §6m — a session on the linear route, bytes in through stdin (§107, ruling 29).
import { describe, it } from "vitest";

describe("C22 §6m — a linear session", () => {
  it.todo("T4.102 (C22 I119, C22 I120, C22 I123, C01 I22): no frame, ruling 29's lines, the input line erased and redrawn — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.103 (C22 I122, C22 I124): a numbered question answered by its number — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.104 (C22 I125, C02 I15): /capabilities shows the route first — not deferred on a component: specified ahead of the code in this commit");
});
