// C22 I117 — the shell keeps a split's panes (C04 §3aq, C26 I28, C16 I59).
import { describe, it } from "vitest";

describe("C22 I117 — split", () => {
  it.todo("T4.96 (C22 I117, C26 I28): the arrows keep to a pane and cross it on ←→ — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.97 (C22 I117, C16 I59): ⌥←→ move the divider, and stay word motion in the prompt — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.98 (C22 I117): the pointer drags the divider — not deferred on a component: specified ahead of the code in this commit");
});
