import { describe, it } from "vitest";

// **Ink ramps, tier 4** (C09 §5 *Ramps*, C22 I60). The rows land with the
// content-aware cadence; until then they are named here so the spec commit and
// the code commit cite the same number.
describe("C09 §5 — a shimmer through the session", () => {
  it.todo("T4.8 (C09 I54, C22 I60): a shimmer on a running head re-renders each spinner commit while a plain entry beside it is served from the cache, read from the cache's counters — not deferred on a component: lands with the content-aware cadence");
});
