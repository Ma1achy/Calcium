// C16 I54, C17 I29, C23 I77 — the typed reply, driven through a built session (§052, R-QST-003).
import { describe, it } from "vitest";

describe("§052 — a question that wants a sentence, through the router", () => {
  it.todo("T4.80 (C16 I54, I8, §052): letters typed into a reply reach the line through the prompt rung, and the answer carries them — not deferred on a component: the compose route lands in the router in the next commit of this MR");
  it.todo("T4.71 (C23 I77, C16 I54, C17 I29, §052): the reply's history and undo are its own, and ↓ never leaves the question — not deferred on a component: the owner's history and the held undo stack land in the next commit of this MR");
});
