// C23 §3a — the expand action reaching a tree's node (C04 §3ap E1).
import { describe, it } from "vitest";

describe("C23 §3a — expand on a tree", () => {
  it.todo("T4.72 (C23 I31, C04 I129): a node two levels down, inside a panel — not deferred on a component: specified ahead of the code in this commit");
});
