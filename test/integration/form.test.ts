// C22 I118 — the shell lends a form field the prompt's editor (C04 §3ar, C16 I60, C26 I29, C17 I29).
import { describe, it } from "vitest";

describe("C22 I118 — form", () => {
  it.todo("T4.99 (C22 I118, C16 I60, C26 I29): a field entered, edited, committed and discarded — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.100 (C22 I118): the borrow ends before a pointer's move or an action — not deferred on a component: specified ahead of the code in this commit");
  it.todo("T4.101 (C22 I118, C16 I60): a refused paste, and a rejected key — not deferred on a component: specified ahead of the code in this commit");
});
