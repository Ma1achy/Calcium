# The rule ledger — every `current` rule, resolved against the tree

**Why this file exists, and it is the eighth blind spot arriving at this
reconciliation's own record.** The sixteen MRs were checked, MR by MR, against
**the plan's** text — the deliverables each one names. The plan is not the
registry. `AUTHORITY.md` puts `status: current` at the top of the precedence
ladder and the plan nowhere on it, so a rule the plan never mentioned could be
unbuilt, uncited and invisible to every per-MR report, and forty-one of them
were. Measured:

    118 current rules in docs/design/language/calcium-registry.json
     70 named somewhere outside docs/design/language/   (cited)
      7 discharged under another name, with the subject  (covered)
     41 resolved by nothing yet                          (owed)

**A count in prose is a snapshot with no mechanism** (F142), which is the
argument for `tools/rule-status.mjs` rather than for this table alone. The tool
re-derives all four figures and fails when any row's claim stops holding.

## The five states — `cited` was carrying three, so two came out of it

**Amended 2026-09-24.** A parked rule, an audited-and-unmet rule and a rule
nobody had checked all read `cited`, because each names its R-ID somewhere. A
sample of five found one whose only citation is a spec row reading *No field, no
painter* (R-HON-002). So `cited` now means **not yet audited**, and the audit's
measure is that count reaching zero. The remainder is `unmet + parked + owed`.

- **`unmet`** — cited, audited, and **not satisfied**. It must be cited (an
  uncited unmet rule is `owed`), and its reason must be anchored to a path, a
  section or an invariant, so *unmet* cannot be written without saying where.
- **`parked`** — the row says `parked as N`, and N is an **open** entry in
  `docs/design/PARKED_QUESTIONS.md`: a question retracted or removed fails the
  row, so a row cannot outlive its question.

The original three follow.

- **`cited`** — the rule's `R-XXX-NNN` appears in `src/`, `test/`, `tools/`,
  `docs/components/`, `docs/architecture/`, `docs/design/layout/`,
  `PARKED_QUESTIONS.md` or `CALCIUM_ROADMAP.md`. The R-ID is the resolution and
  nothing more is claimed here: **cited is not the same as satisfied**, and this
  file does not pretend otherwise. What it rules out is a rule nothing in the
  repository has ever looked at.
- **`covered`** — the rule is discharged by a mechanism that predates it and
  carries another name, so no R-ID appears and none should be added merely to
  make a table green. The row names the subject, and the tool resolves it: every
  path must exist and every identifier must appear in a file the row cites,
  which is `roadmap-status.mjs`'s check and the one that caught a row naming a
  real mechanism in the wrong file.
- **`owed`** — resolved by nothing yet. **This is a remainder, not a
  divergence.** Nothing here is recorded as knowingly divergent; each is work
  the reconciliation has not reached, and the row is what makes it countable
  instead of invisible.

## The check that keeps this honest

An `owed` row whose rule **has** since been cited is a failure, not a pass. A
ledger that only checked its positive claims would certify whichever subset
chose to carry one, and the stale half is the half that reads as coverage —
`roadmap-status.mjs` §2's argument, in the other direction. So the three sets
partition the 118 **by equality**, both ways, and a rule that lands fails here
on the day it lands until its row moves.

**What this file does not check**, stated rather than left as an omission: a
`cited` row proves a reference, not a behaviour. The instrument that asks
whether a rule is *satisfied* is the carrier matrix, the golden frames and the
mutation runs — this one asks the prior question, which nothing asked before.

---

| rule | state | resolved by |
|---|---|---|
| **R-SPC-001** — Registry authority | `covered` | **Audited 2026-09-24.** `docs/design/language/check-calcium.mjs` `checkGeneration`, `checkImmutableBlockReconciliation`, `checkImmutableRuleRecords` · `docs/design/language/build-calcium.mjs` `validateRuleRecords`, `assertCurrentCitations` · `make design-check`. The HTML is rebuilt in memory from the registry and must equal the committed projection; artefacts filter `status === 'current'`; a reorder keeps stable IDs; superseded records keep an anchor and a replacement link. It runs under `make enforce`, and so in the pre-commit hook, not as a vitest row. |
| **R-COR-001** — Three histories | `covered` | C13 I20 · `src/shell/transcript-persist.ts` `PersistFs` · `src/viewport/transcript/cap.ts` `MARKER_ID`, `countBlocks` · `test/unit/transcript-persist.test.ts` T1.28 · `test/unit/block-cap.test.ts` T3.22 — **the earlier row said the scope question had to be answered first and never asked it.** Asked: two of the three histories are this repository's and both are built, and the third is the application's by the design's own sentence. *The durable event record* is the resume file, and its header states the property the rule needs — **only settled entries are written, which makes the file append-only in fact rather than by assumption**. *The rendered window* is the store under its block cap, and eviction leaves `transcript:evicted` above what survives, which T3.22 asserts with two caps and two markers. **And the two cannot touch**: nothing in the persistence module names eviction, the sweep or the marker, so *eviction changes the window, and neither erases history* holds by the file having no code path that could shrink it. *The model context* is out of this repository's authority — §065's own words are **only a harness holds the turns to say it**, and compaction, turn counts and token budgets are the harness's throughout |
| **R-COR-002** — Render does not seize ownership | `covered` | **Audited 2026-09-24.** C16 I15 · `src/interaction/router/focus.ts` `activeTarget` · `test/unit/router-focus.test.ts` T1.3, T1.3g. The owner is computed from its inputs and `liveEntry` does not move it; T1.3g differs only in `liveEntry` and asserts the rung holds, with a question overlay as the control that does raise it. Every writer of the focus store is a key or click handler. Limit: nothing scans for a future render path that writes the store. |
| **R-COR-003** — Independent carriers | `unmet` | **Audited 2026-09-24.** **three facts have one carrier and the gate reads prose.** C10 §4k.5 records selection *ground alone* (parked as 33 — the design says both), choice *mark alone*, disclosure one mark; T2.57 (`test/contract/theme.test.ts`) reads the spec's table, not a render. At 1-bit `focusStyle` (`src/presentation/blocks/paint.ts`) gives nothing and `▸` is drawn only by table rows and the tape, so `choice`, `control`, a mosaic pane and a notice head draw focused and resting alike. The readout clause is checked by nothing. |
| **R-STA-001** — Orthogonal state axes | `unmet` | **Audited 2026-09-24.** **a generic active flag is stored.** `Pills` chips carry `active?: boolean` (`src/data/viewmodel/types.ts`), which the completion menu (`src/interaction/completion/menu.ts`) sets for its highlighted candidate and `pills` paints accent — one boolean doing focus's or selection's work. T2.52 compares the registry's axes with C10's prose and cannot see a field. |
| **R-STA-002** — One ground per cell | `unmet` | **Audited 2026-09-24.** **no single ground resolver, and the displaced fact's carrier is asserted nowhere.** Selection over a structural chip is held (`src/shell/paint.ts` `chipRanges`, T1.68) but T1.68 asserts one ground, not what the displaced chip keeps. Focus + selection (T2.45) is `it.todo`; owner/armed and hover preview have no ground subject; selection over a diff is unconstructible because `patch` declares no elements. |
| **R-STA-003** — Hover is not focus | `unmet` | **Audited 2026-09-24.** **hover is held; focus's persistent non-colour carrier is not.** Clause 1: T4.72 (`test/unit/session-mouse.test.ts`). Clause 2: at 1-bit no `▸` or weight marks a focused `choice` or `control`, a mosaic pane, a notice head, or a button — `buttonSpans` (`src/presentation/blocks/kinds/simple.ts`) draws `[ Approve ]` identically where §017 draws *brackets and bold* — nor eleven plot forms (`SILENT`). T2.46 is `it.todo`. |
| **R-OWN-001** — Owner routing verdicts | `unmet` | **Audited 2026-09-24.** **the question handles what it should reject.** `classify` in `src/shell/confirm.ts` returns none for a non-answer key and the default arm consumes it, so the router records a handle; §103 says a blocking question REJECTS unrelated typing and explains. No handler in `src/` returns `reject`, and `global-intercept` is a `Verdict` member nothing produces. The intercept table and one-acts hold (T1.7, T1.8, T1.30). |
| **R-OWN-002** — Owner epochs | `covered` | C16 I44, I56 · `src/interaction/router/router.ts` `observeArming`, `childAttached` · `test/contract/surface.test.ts` T1.106c · T1.98, T1.99c, T1.102. **Built 2026-09-24.** The epoch half held; the exit arm did not — it read *⌃c at an empty prompt* and an attached child leaves focus there, so the second ⌃c inside the window raised the host's confirm and never reached the child (measured: the second dispatch's stages were `["arming"]` alone). The arm now reads the fact the `child` rung reads. T1.106 was titled for ⌃c and sent none; T1.106c sends two, with the unattached control. |
| **R-OWN-003** — Pointer commit | `covered` | **Audited 2026-09-24.** C16 I45, I46 · `src/interaction/router/router.ts` `armPointer`, `commitPointer`, `cancelArmOnPress`, `pointerArm` · `src/shell/construct.ts` `pointerEffect`, `armActivation`, `armId` · `test/unit/router-dispatch.test.ts` T1.100, T1.101, T1.101b, T1.102, T1.102b · `test/unit/session-mouse.test.ts` T4.64. The press stores an id triple and the epoch; the release commits only on the same triple in the same epoch, after `syncOwner`. Limit: no row re-renders under a held button. |
| **R-QST-001** — Question form and placement | `unmet` | **Audited 2026-09-24.** **blocking is declared; the owner is inferred.** `blocking` and `dismissal` are the layer's (`src/viewport/overlay/types.ts`, T1.29), but no question declares an owner — the router infers the rung from `overlayAnswerCallback()` in `src/interaction/router/router.ts`. Whether a caller may override replace/float is C23 I73's *never declared by its caller* against §051's *placement is a parameter*. |
| **R-QST-002** — Inspection suspends | `unmet` | **Audited 2026-09-24.** **answered is the only outcome.** `AskAnswer` (`src/shell/local/registry.ts`) is `{ key, text? }`; nothing expires a question, and nothing settles one whose asker is gone — `ConfirmHost` (`src/shell/confirm.ts`) has no cancel path, so a promise can hold the keyboard for an owner that no longer exists. §106 draws `○ the question expired · …`. Suspension without settlement holds (T1.70). |
| **R-QST-003** — Editor state isolation | `covered` | **the second owner existed, and so did the leak — behind a reply nobody could type into.** `createEditor` is still called once (`src/shell/construct.ts`), which is §052's *no second implementation*; the second owner is the typed reply M15 landed, and §052 names what it owns — *its OWN buffer, selection, history and undo* — and what it shares — *paste rules, ⇧⏎ newline and ⌥← word move*. Measured against a built session, three things were wrong and the first hid the other two. **Typing never reached the reply**: `confirm.ts`'s compose arm returned `false` on the strength of a ladder fall-through `src/interaction/router/router.ts` has never had, so every letter met the modal reject and the answer was always empty (C16 I54). **The undo stack was shared**: `holdDraft` entered the borrow through `setText`, which records a unit holding the reader's line, and the reply's own units outlived `restoreDraft` (`src/interaction/editor/editor.ts`, C17 I29). **The history was the prompt's**: `↑` in a reply walked the command history and `↓` past its floor entered the live block from under an unanswered question (C23 I77). Each is now held by its own invariant and a row driven through the router rather than through the mechanism |
| **R-QST-004** — Single approval overflow path | `unmet` | **Audited 2026-09-24.** **the framework's approval has no inspection.** `approvalPrompt` with `APPROVAL_CHOICES` (`src/shell/documents.ts`) offers allow and deny, carries no detail and no `show full diff`; the mechanism (`Choice.inspect`, `suspend`) exists only when a caller supplies it (T1.70b builds its own list). §051 also puts `no` first and focused where the tree puts allow first. |
| **R-MOT-001** — Motion preference | `unmet` | **Audited 2026-09-24.** **three motion carriers never see the preference.** The call card's duration spinner and the operation head's walk are composed in `src/shell/documents.ts` (`spin`, `operationSpin`) from the readout's tick, and an animated image advances in `src/shell/session.ts` with no motion gate. T2.166 has no ASCII corner, and nothing tests `TuiConfig.motion` reaching a frame. |
| **R-MOT-002** — Spinner timing | `unmet` | **Audited 2026-09-24.** **the call card's spinner is a 1000 ms frame and keeps moving under off.** `src/shell/refresh.ts` hands `Math.floor(since / 1000)` to `spin` (`src/shell/documents.ts`), so braille — 80 ms in the registry — steps once a second, as C23 I58 rules; and no motion value reaches the card. T2.167 renders a `status` only. |
| **R-MOT-003** — Reduced-motion mapping | `unmet` | **Audited 2026-09-24.** **the product half holds; the preview half is checked by nothing.** C09 I99 · `effectiveAnimation`, `AMBIENT_ANIMATIONS` (`src/presentation/blocks/ramp.ts`) · T2.168. The preview's `prefers-reduced-motion` → off mapping lives in the registry's shell CSS, and `docs/design/language/check-calcium.mjs` asserts nothing about it. |
| **R-MOT-004** — Semantic frame contrast | `unmet` | **Audited 2026-09-24.** **no frame of a ramp is checked against a floor.** `mixHex` (`src/presentation/theme/ramp.ts`) interpolates two tokens with no floor on the mix, and nothing pairs a ramp frame with a surface. `fade`'s head is `muted` (floor 2.5) on the sole carrier — §026 draws it at the ground, which is the contradiction parked as 34. |
| **R-MOT-005** — One-cell animation carriers | `covered` | **Audited 2026-09-24.** C09 I107 · `src/presentation/blocks/glyphs.ts` `spinnerFrames`, `SPINNER_SETS`, `narrowOnly` · `src/presentation/blocks/ramp.ts` `animateT` · `test/contract/spinners.test.ts` T2.70, T2.169, T2.163 · `test/contract/ramp-registry.test.ts` T2.117c, T2.117e. Every frame is one cell (T2.70), the ASCII rung is taken whole (T2.169), and a ramp is a colour sample per cell (T2.117c); the specimen CSS animates colour only. |
| **R-PRG-001** — Meter semantics | `covered` | **Audited 2026-09-24.** C09 I97 · `src/data/viewmodel/types.ts` `granularity?:`, `liveness?:` · `src/presentation/blocks/kinds/simple.ts` `block.granularity`, `block.liveness` · `test/contract/blocks.test.ts` T2.162. Three independent fields, each with one consequence; T2.162 drives each alone and a triple that is neither preset. |
| **R-TXT-001** — Text coordinate systems | `covered` | `src/interaction/editor/graphemes.ts` `sliceBetween` · `src/interaction/parser/classify.ts` `tokens` · `src/presentation/text.ts` `cells` — the four systems are four modules and the editor's own header says so: *`graphemes` takes text apart, `words` classifies it, `layout` places it on* cells. Editing goes through `sliceBetween`/`splitAt`, which are grapheme-indexed, so a public span cannot land inside a cluster |
| **R-GLY-003** — Glyph registry and reserved cells, which bind in fixed columns | `unmet` | **Audited 2026-09-24.** **two ASCII fallbacks differ from the registry and nothing reads `reservedCells`.** `src/presentation/blocks/glyphs.ts` draws `continuation` as `` ` `` where the registry's `branch` is `` `- `` at two cells, and `hollow` as `o` where `choice-open` is `@` — while C09 I105 says the marks are the registry's already. `glyphLead` and `gutterCell` pad nothing. Owed: the two characters, padding to the reservation, and a row comparing each registry glyph's `ascii` and `reservedCells` with the tree's. |
| **R-CAP-001** — Capability vector | `covered` | **Audited 2026-09-24.** C02 I12 · C16 I35, I36 · `src/interaction/router/keymap.ts` `defaultKeymap`, `mergeBlock` · `src/interaction/router/types.ts` `KeyProfile` · `src/terminal/capabilities.ts` `TerminalCapabilities` · `test/unit/router-keymap.test.ts` T1.35, T1.36. Capabilities are independent fields; an enhanced row is never an action's only route (T1.36), and an adapter keymap cannot introduce an action (I19). Residue: T1.36 checks per action rather than per target. |
| **R-ACC-001** — Linear semantic rendering | `parked` | **one of §107's nine fields is built and it is the id.** The element record is `{id, level, rows, cols}` (`src/presentation/blocks/types.ts`) — a stable id and a rectangle, where the design's semantic node wants *id, role, name, description, value, state, position, actions, relations*, and `level: "block"|"row"|"cell"` is a geometry hint rather than a rôle. The two renderings — `rich` and `linear`, the second an append-only stream selectable by `--linear` or `CALCIUM_RENDER_MODE` — do not exist, and neither does the announcement policy (`none`/`polite`/`assertive`, deduplicated and rate-limited, *from semantic events, never from diffing the screen*). **One §107 sentence is already held**: *if a patch removes the focused node, focus re-resolves by the declared stable-id fallback* is R-PTR-002's `resolveFocus`, covered above **Parked as 29** (`docs/design/PARKED_QUESTIONS.md`): the role vocabulary for the kinds §107 does not name and the text of a linear event are wording the design does not give, and mode selection inherits 28 |
| **R-TRU-001** — Terminal control boundary | `covered` | **Audited 2026-09-24.** C09 I89 · C04 I110 · C17 I9 · `tools/enforce/source-scans.mjs` `SS14` · `src/data/text.ts` `stripControl` · `test/unit/trust-boundary.test.ts` T2.156, T2.156d. SS14 refuses ESC in `src/` outside `escapes.ts`; T2.156 plants SGR, erase, OSC and C1 in every string field of every kind and asserts no control reaches the frame while the printable residue does; a terminal line with a control byte fails validation. |
| **R-THM-001** — Theme tokens | `covered` | **Audited 2026-09-24.** `docs/design/language/build-calcium.mjs` `themeCss`, `themeControls` · `docs/design/language/check-calcium.mjs` `checkGeneration` · `make design-check`. Theme CSS is emitted from current `themeRules` only and rebuild equality catches drift. Beside the rule's text: `src/presentation/theme/tokens.generated.ts` has no regenerate-and-compare gate; value rows (T2.53) hold it. |
| **R-PRI-001** — Primitive index | `covered` | `docs/design/language/build-calcium.mjs:412` — the builder throws when a primitive lacks `definedIn`, its canonical definition links, and `make design-check` runs it. The index is generated from the registered primitives rather than written beside them, which is the rule enforcing itself in the artefact it is about |
| **R-TAB-001** — Component state tables | `covered` | **Audited 2026-09-24.** `docs/design/language/check-calcium.mjs` `checkStateTables`, `STATE_FACT_KEYS` · `docs/design/language/build-calcium.mjs` `assertStateLines` · `make design-check`. The builder throws unless every current component has exactly the seven facts; the checker refuses leftover raw HTML and checks the rendered counts. |
| **R-PER-001** — Permission posture registry | `parked` | **§088 draws the display this rule is about, and it is a surface the tree has no equivalent of.** `/permissions` is a table — *what · where · granted* — with rows like `rg --stats -uu · the count agent · 22:41` and a key line `↑↓ move   ⌥⌫ revoke   ⌃⇧C copy`, and the argument on the fixture is the rule's own: *always allow is a decision, and a decision you cannot see is one you cannot revoke*. So *explicit, inspectable and revocable* are three separate claims and the tree satisfies none: nothing stores a standing grant, nothing lists one, nothing revokes one. **And it stays blocked, parked as 18, rather than merely unbuilt** — the posture vocabulary is the two normative sources' disagreement, so a registry built now would have to choose between `skip` and `ask` to name its rows |
| **R-STR-001** — Call shape | `covered` | `src/shell/documents.ts` `callHead` and `callBody` — the head is one block, the gutter is `src/presentation/blocks/kinds/simple.ts` `glyphLead`'s two cells, and `callBody` emits the settled `notice` plus **at most one** `scroll`, guarded by `call.output !== undefined && call.output.length > 0`. *At most one bounded body when output exists* is that condition |
| **R-STR-002** — Sibling spacing | `covered` | C09 I17 · C22 I85 · `src/shell/builders/index.ts` `gapBefore` · `src/shell/entry-layout.ts` `ENTRY_GAP` · `test/unit/frame-budget.test.ts` T1.45 — **both halves are built and the earlier row searched the wrong files.** It looked in the shell's document and construction files and concluded the transcript inserts no row; the half it missed is `ENTRY_GAP = 1`, *the blank row that closes every entry* — **the entry's own**, so C14 measures it with the entry rather than the loop inserting it between two, which is why a search of the loop found nothing. T1.45 asserts it measured, drawn, windowed and element-free. *Spacing inside a body belongs to that block* is the other half: `finish` resolves `gapBefore` to `padding: {t: 1}`, with the block marked when the value was a default and left alone when explicit, so `b.seq` touches only the blocks that were never asked |
| **R-STR-003** — Width-bounded nesting | `unmet` | **the figure was checked and the design does not draw it, which parks the first clause as 25.** Over all 130 fixtures, every `⎿` row against the head above it: **28 instances at a two-column step and none at three**; the nine remaining are step 0, where the line above is the `✦` activity line and is not a parent. `GLYPH_INDENT`/`prefixCells` (`src/presentation/blocks/kinds/simple.ts`) give a head two cells and `⎿` four, so the tree draws the same two-column step the kit's own frames do — by arithmetic, since `prefixCells` *is* the hanging indent. The contradiction is 28 figures against one sentence and amending a `current` rule's text is not mine to take. The second clause — nesting limited by width while the deepest child clears its minimum — is untouched by the answer and stays owed on its own account **Audited 2026-09-24: the second clause is unbuilt.** Nothing in `src/presentation/blocks/kinds/containers.ts` limits nesting by width — a `panel` insets by its border at every depth and hands its child whatever is left, so at depth the deepest child is drawn below any minimum rather than the nesting stopping first. Owed: a container that sheds its frame when the inset would take its deepest child below that child's `width` answer (C09 §2c) |
| **R-STR-004** — Inline-end breathing cell | `covered` | C22 I109 · `CONTENT_MARGIN_R` — `region.width = max(1, size.columns - 1)`, with the three exemptions each already an invariant (C22 I81, C22 I86, C22 I87) |
| **R-COL-001** — Red semantics | `parked` | **four clauses, three of which resolve and one blocked on parked 18.** *Red is never the only carrier* is C10 I56's carrier table, whose test of independence is the 1-bit rung. *Removal* keeps its role in C25's patch renderer and *identity* in the hue palette, which C10 I55 declares `carries: "decoration"` — a hue is not a status. **The unresolved clause is *override red means unsafe override***: the permission postures it belongs to are the two normative sources' disagreement, parked as 18, so the second red has no slot to be declared in yet |
| **R-COL-002** — Regional tone budget | `covered` | C10 I58 · C10 commitment 49 · `src/presentation/theme/budget.ts` `entryTones`, `TONE_BUDGET`, `TONE_SMELL` · `test/unit/tone-budget.test.ts` T1.40, T1.41, T1.42, T1.43 — **two-thirds shipped under another name and the missing third was the budget itself, which is now counted.** `PaletteSpec.carries: "meaning" | "decoration"` (`src/presentation/theme/types.ts`) is the regional mechanism and `src/presentation/theme/tokens-dark.ts` declares four palettes beside each other — `tone` (10, meaning), `syntax` (9, meaning), `categorical` (8, decoration), `spectrum` (9, decoration) — so *code carries its own adjacent palette* is `syntax` and *plots* is `categorical`, both already built. Patches took a different shape: `diffAdd`/`diffRemove` are **surfaces**, grounds on the row rather than an indexed ink palette, which answers the third region by a ground. **The clause with no subject was the count**: `tone` offers ten slots and no site limited what one entry spent. §090 settles it in one line — *an ordinary entry: three semantic tones. Five is a smell* — and gives the scope in its own words, *a full frame counting twelve tones is not breaking the rule, the rule was about one ENTRY, and it was stated globally*. So the budget is three, the gate is five, both kept as named constants because they do different jobs; the count is per entry and never per frame; and `code`, `plot` and `patch` are skipped **whole**, since a region's palette lives inside it. **Measured**: the whole of `ONE_PER_KIND` as one entry spends three — `info ok muted` — so the gate is green because the tree is inside the budget, and T1.41 carries a fabricated six-tone entry so that it is not green because nothing is counted. **Stated blind spot, larger than it first read**: neither corpus puts an entry near the threshold. The fixtures spend three between all of them, and the docker example's **38 documents are none above three**, nine of them spending none at all because they are `raw` passthrough blocks rather than the table `examples/docker/src/ps.ts` tones by container state. The gate has counted and found nothing over — a measurement, not a vindication — and the only thing showing it can fire is T1.47's fabricated six-tone entry. An entry a consumer builds is reached by nothing here and cannot be, because *five is a smell* is a judgement about a design and a constructor refusal would turn it into an error |
| **R-COL-003** — Ground and mark | `covered` | **Audited 2026-09-24.** C09 I95 · C22 I111, I114 · `src/shell/paint.ts` `labelSpansOf` · `src/presentation/blocks/kinds/status.ts` `surface.errorGround` · `test/contract/blocks.test.ts` T2.159 · `test/unit/frame-rule-label.test.ts` T1.65b, T1.71. T2.159 renders every kind and asserts, by equality both ways, which paint a background — the extents and the error tag; the prompt rule's label is a ground (T1.65b). Limit: at rest and at 24-bit only. |
| **R-COL-004** — Ground redundancy | `unmet` | **Audited 2026-09-24.** **interaction grounds carry alone.** Selection (parked as 33), a focused mosaic pane (`paneGround`, `src/presentation/blocks/kinds/containers.ts`), and the focus wash on `choice` and `control` have no second carrier at 1-bit. *Dense data owes a readout* is checked by nothing; T2.159 carries the R-ID and checks which kinds paint a ground. |
| **R-COL-005** — Shape-specific focus | `unmet` | **Audited 2026-09-24.** **one shape of five is built.** Control washes (T1.73). A run does not invert; a box inverts to `pick` whatever its tone (`buttonSpans`, `src/presentation/blocks/kinds/simple.ts`) where §017 inverts Deny to its tone; `panel`, `smallmultiples` and `pairplot` take no border and a mosaic pane takes a ground where §018 figure 5 draws `┏━┓`; a painted identity chip neither bolds nor marks. |
| **R-MOT-006** — Semantic motion | `covered` | C09 I99 · `src/presentation/blocks/ramp.ts` `AMBIENT_ANIMATIONS`, `effectiveAnimation` · `test/contract/spinners.test.ts` T2.168 — **the earlier row said nothing names it and that was wrong.** The attention group *is* what an animation says: `rampPolicy.attentionGroups` splits the ramps into `ambient` — `glint`, `drift`, `tide`, the three the design calls decorative — and `working`, `waiting`, `attention`, `terminal`, *each of which says something a reader is meant to act on*. `reduced` stops exactly the ambient three and keeps the other four, which is the rule's two halves made operational: motion that only says *look here* is the group a preference can silence, and motion that says what is happening is the group it cannot. The set is the registry's own `attentionGroups` and T2.168 compares the two **by equality**, so a fifth arriving in the registry fails rather than passing unseen |
| **R-MOT-007** — Duration-independent shape | `covered` | C09 I8 · `src/presentation/blocks/kinds/containers.ts:135` — *`measure` never sees the tick*, so a block's geometry cannot vary with elapsed time. The call's shape is its measured rows, and the duration reaches only the spinner and the tone |
| **R-MOT-008** — Agent bloom ownership | `unmet` | **Audited 2026-09-24.** **nothing stops a tool blooming.** `Status.spinner` accepts any set (`src/data/viewmodel/types.ts`, unvalidated); `TAPE_SPINNER` (`src/presentation/blocks/kinds/tape.ts`) is `agent` for every running member; `operationSpin` (`src/shell/documents.ts`) blooms any verb. T2.164 checks other sets' frames, not where a set is chosen. |
| **R-INT-001** — Scopes are visible | `unmet` | **Audited 2026-09-24.** **the scope line is drawn per rung, not per scope.** `ownerChips("scope")` in `src/shell/chrome.ts` always returns the prompt's keys, so with an entry or a row focused the footer says `⏎ send`; §016 draws `↑↓ move ⏎ open ⌥⇧V copy esc back` and `↑↓ rows ⇧↑↓ select ⏎ activate esc back`. `substate` always reads `find`. T1.46e sees one line per rung. |
| **R-INT-002** — No typing-scope single keys | `covered` | **Audited 2026-09-24.** C16 I52 · `src/interaction/router/keymap.ts` `defaultKeymap`, `createKeymap` · `test/unit/router-keymap.test.ts` T1.158. The router's one keymap is built from `defaultKeymap`; T1.158 classifies every target by equality — no typing scope binds an unmodified single character — with the non-typing scopes, which do (`y`, `?`), as its control. |
| **R-INT-003** — Scroll is not focus | `covered` | C26 §7a · `src/shell/construct.ts` `ScrollOffsets` — the offset is view state of its own, so a scroll writes `ScrollOffsets` and never the focus address; §7a carries the sentence in its heading |
| **R-INT-004** — Minimum focus reveal | `covered` | C26 §7a · `src/shell/pull.ts` `pullIntoView` — the file's own opening line is *FOCUS PULLS THE VIEWPORT, BY THE MINIMUM, one row for one row*, and `pullIntoView(held, from, to, window)` is the minimum computed as one number |
| **R-INT-005** — Continuous control commit | `covered` | **the chain is built as three links and the rule has a violation to be about.** §102 settles all three links — *a block declares whether it has VIEW STATE* is a view-state member on `NavElement` (`src/presentation/blocks/types.ts`), *focused — the way IN — ⏎ enter* and *esc out* are `rowActivate`/`Esc` calling `setMode` (`src/interaction/router/focus.ts`), and the bridge is `focusFor` reflecting the mode into `FocusState`'s `inside`, which `isInside` in `src/presentation/blocks/kinds/controls.ts` has read as constant `false` since it was written. C26 I26 holds the chain and **C26 I27 holds the gate**: every binding that moves a camera, a cursor or a handle sits at the `interaction` target and nowhere else. It was not vacuous when it landed — the camera family was bound at `liveBlock`, nine rows in `src/interaction/router/keymap.ts`, and the orbit action reached `orbitBlock` in `src/shell/construct.ts`, which is a domain value committed from outside on the one subject the rule has. **The design overruled a spec to get here**: C26 I2 gated `interaction` on the focused entry being live, and §102's *VIEW STATE IS NOT LIVENESS* withdraws it (§8b.9). The chords are §102's own — `←→ orbit   ↑↓ tilt` — so `[` `]` `{` `}` retire with the target that forced them (C22 I75, C16 I28) **And building it found two seams nothing was watching**: the `interaction` rung had **no registered handler at all** — a target in the ladder since it landed, with nothing to read its table, invisible while the rung was unreachable and while it held no rows — and a block's free keys merged at `liveBlock` alone, so entering a plot to orbit it silently took away its series digits. Both are C16 I27's own silent-shadow hazard arriving from a side that could not be stood on until the entry existed |
| **R-INT-006** — Pointer parity | `covered` | `src/shell/construct.ts:2962` — the pointer's gesture table onto the key effects (C16 §4a, C16 I31), and its header states the rule: *nothing here can store a location the keyboard could not*. Every arm lands through the same `focus` calls and the same `keys.table` effects — a click is `↓`/`tab`'s state, click-again is `⏎`, a drag is `⇧↓`, the wheel inside a box is `PgDn` on it — so parity is **structural rather than remembered** and there is no second focus mechanism to drift |
| **R-INT-007** — Escape owner path | `unmet` | **Audited 2026-09-24.** **esc at a prompt with text does nothing.** §016's third step — *the prompt has text → clear the line* — has no row: `defaultKeymap` (`src/interaction/router/keymap.ts`) binds no `prompt` escape, so the key falls to `global` unbound. The child and question halves hold (T1.33, T2.15, T1.41b). |
| **R-INT-008** — No unsolicited reader motion | `unmet` | **Audited 2026-09-24.** **a rejected key at a question explains nothing.** §061 case 2: *the first keystroke that is not an answer puts that notice on the question, once* — `▲ answer this first`, warn. `src/shell/confirm.ts` consumes it silently and the string is nowhere in `src/`. Hover is passive (T3.8d). |
| **R-HON-001** — Dash is unknown | `covered` | `src/presentation/plot/ramp.ts` `absent` — the ramp alphabet carries `absent` as a member beside `empty`, `—` U+2014 dropping to ASCII `-`, with the reason on the line: *there is no braille glyph that reads as nothing was reported*. An unreported value and a value of zero take different marks by construction |
| **R-HON-002** — Staleness timestamp | `covered` | C04 I127 · C09 I109, C09 I110 · C10 I59 · C23 I35, I78 · `src/presentation/blocks/kinds/containers.ts` `staleNotice`, `age`, `recede` · `src/shell/refresh.ts` `staleAge`, `staleFigure` · `src/presentation/theme/resolve.ts` `recedes` · `test/unit/stale-reading.test.ts` T1.51, T1.74, T2.172, T2.173, T1.50 · `test/contract/refresh.test.ts` T1.36, T1.73, T1.74. **Built 2026-09-24, and three things were wrong where the row said nothing existed.** The driver wrote `· 240s ago` into the title once, at onset, and never again; a hung fetch never went stale because the sweep skipped in-flight sources; and the draw was not §047's. Now the panel carries `staleForMs`, C09 draws `updated 4m ago` at the inline end in warn with the title yielding first, the children render under `recede(theme)` so the content dims and the chrome does not, and the figure advances when it would change. Mutated: `c09-stale-reading` 11 of 11, `c23-refresh` 17 of 17 |
| **R-HON-003** — Bounded-window residue | `covered` | C04 I49 · `src/presentation/blocks/kinds/containers.ts` `residueRow` — *a bounded window says what it dropped* is the residue row, `⋯ N above, M below` / `⋯ +N more`, measured at `height+1` independent of the offset |
| **R-HON-004** — Refusal reason | `unmet` | **Audited 2026-09-24.** **the question's refusal states no reason.** `src/shell/confirm.ts`'s default arm and the router's `modal-blocked` both refuse silently; §061's notice is `▲ answer this first` in warn. Where refusals are drawn they are not red (`refusalNotice`, `src/shell/documents.ts`), and no row pins that tone. |
| **R-HON-005** — Numbers have units | `covered` | `src/presentation/blocks/kinds/status.ts` `elapsed`, `countdown` · `src/shell/chrome.ts` `formatFrameCost` · `test/edge/status.test.ts` T3.42a — **the earlier row stopped at a homonym and never counted the population.** *unitsOf* really is a table's divisible units and not a number's; what it did not ask is how many numbers the framework draws at all. **Three, and all three attach the unit**: `elapsed` returns `4s` or `2m 30s`, `countdown` is drawn as *retrying in 12s*, and `formatFrameCost` returns *last 9.6ms* with its reason on the line — *one decimal, and the unit attached*. T3.42a asserts the drawn row as `loading (4s)`, so the unit travels into the frame rather than stopping at the formatter. **The residue is a table cell and it is already held elsewhere**: a number arriving from the far side has no unit to travel with it, because ColumnDef carries no unit field — which is R-TBL-004's second clause and is owed there, not twice. A count the framework draws bare — `⋯ +5 more`, `⋯ 3 above, 7 below` — is not a violation: the design draws the same figure the same way in §095 |
| **R-HON-006** — Copy source | `covered` | `src/shell/semantic-selection.ts` `copyTextOf` — it takes the entries' `Block`s and hands them to `copySequence`, the per-kind copy form (R-SEL-004). The frame is never a parameter, so *the source, never the rendering* is a property of the signature rather than of a rule the copy path follows |
| **R-HON-007** — Histories stay distinct | `covered` | C13 I20 · C14 I24 · `src/viewport/transcript/cap.ts` `MARKER_ID` · `test/unit/block-cap.test.ts` T3.22 — R-COR-001's subject from the honesty side, and the marker is the mechanism: the window shrinking is **said** rather than silent, so the visible window cannot pass itself off as the record. T3.22 is the sharp case — *two caps, two markers*, the block cap's notice above and the row cap's beneath — which is two windows each declaring its own loss rather than one summary standing for both. The model context is the harness's here as it is on R-COR-001, so *never masquerade as one* has two subjects in this repository and both declare themselves |
| **R-DEG-001** — One-bit survival | `covered` | C10 I56 · §4k.5's carrier table — *every distinction survives to one-bit* is the table's own test of independence and its only one: `mark`, `word`, `weight`, `position` and `inverse` survive the rung, `tone` and `ground` do not, so a pair drawn from the second set is one carrier written twice. Twelve rows, each traced to a field and a renderer, held to the registry's axes by equality (C10 T2.57) |
| **R-DEG-002** — Capabilities enhance | `covered` | `test/unit/router-keymap.test.ts` T1.36 (keys) · A03 SS65 (glyphs) — *a capability makes things better, never possible* in its two halves. T1.36's own words are *the enhanced rows are additions, never the only way*, over every action in the table; SS65 holds every `current` registry glyph to a mark this tree can draw, and 16 of the 17 declare an ASCII fallback. **The seventeenth is the rule kept rather than broken**: `work-unit` carries `asciiResolution: "state"` with its reason — above 1-bit the head mark is one glyph and tone carries the state, at ASCII the state carries itself, and pinning `o` would have spent the mark `choice-open` needs |
| **R-DEG-003** — Set-level width check | `covered` | C09 I93 · `test/unit/blocks.test.ts` T1.61 — the row is named *the set degrades whole, and the check is on the set*; `scrollbarSet` returns one whole set or the other, so no member can take an ASCII rung alone |
| **R-INT-009** — Immediate feedback | `unmet` | **Audited 2026-09-24.** **two silent handles.** A non-answer key at a question (`src/shell/confirm.ts`), and fourteen reserved chords consumed at `global` and doing nothing visible (`reserved`, `src/shell/keys.ts`), which T1.38 asserts. The question's notice is settled by §061; the reserved chords' wording is not. |
| **R-INT-010** — Reversible action | `covered` | `src/shell/chrome.ts` `shedToWidth` — the footer sheds from the right and keeps **two** chips at every width: chip 0, the owner, and the way out, found by name (`esc`, `host escape`). So the safe exit survives the narrowest responsive rung by construction rather than by a caller remembering to draw it, and where a rung has no escape the second survivor is its primary action — §103's order read literally |
| **R-KEY-002** — Repeat is scoped and declared | `covered` | C16 I53 · `src/interaction/router/router.ts` `repeatSteps`, `repeatFor` · `src/interaction/router/repeat.ts` `REPEAT_POLICIES` · `test/unit/router-dispatch.test.ts` T1.159f · `test/unit/router-keymap.test.ts` T1.159–T1.159e. **Built 2026-09-24.** The policy was looked up at the active target alone, and ⌥↑/⌥↓ are bound only at `global`, so a repeat 100 ms after the press acted where §020 declares 250 / 90 (measured). The lookup is now dispatch's order — target, then `global` — and T1.159f is the first row to send a `repeat` through `dispatch`, with an undeclared prompt key as its control. `c16-key-repeat` 3/3; I53 had no mutation run before this. No tier-5 row sends a real kitty repeat. |
| **R-MOT-009** — Ping-pong endpoints occur once | `unmet` | **Audited 2026-09-24.** **the data holds and the check reaches one endpoint.** Every ping-pong set runs 0 → N → 1 with no repeat; T2.165 (`test/contract/spinners.test.ts`) asserts the wrap and computes `peak` only to discard it (`void peak`), so a doubled peak fails nothing. |
| **R-MOT-010** — ASCII motion preserves semantics | `covered` | **Audited 2026-09-24.** C09 I98 · `src/presentation/blocks/glyphs.ts` `SPINNER_SETS`, `spinnerFrames` · `test/contract/spinners.test.ts` T2.163, T2.73. Each ASCII rung is the registry's `asciiPattern` fitted by the design's own function (T2.163), and its cycle equals the Unicode one (T2.73). |
| **R-MOT-011** — Shared alphabets share timing | `unmet` | **Audited 2026-09-24.** **one tick drives every set.** `src/shell/session.ts` runs at the fastest on-screen interval and every set steps on it, so `toggle` (400 ms) strobes at 80 beside a braille status — a family's rate varied at runtime. The alphabet clause is parked as 30: the registry's own ASCII rungs share alphabets across intervals. T2.73 cites this rule and asserts R-MOT-010's. |
| **R-NTF-001** — Watched completion earns notification | `parked` | **parked as 27, on three counts, and the row that called this *the clearest unbuilt feature with no visible choice open* was wrong on the last part.** It is the **only** `current` rule in the registry about watching, and everything that would give it a subject is `status: example`, which `AUTHORITY.md` says does not bind. **The earning table exists twice and they differ**: §014 has four rows including *the model failed — always*; §088 §3 revises to five, adding *a watched run ended* and *a watched run failed* as **new**, and drops *the model failed* without saying so — a decision and an omission read identically. **The three rungs have no record**: no glyph, delimiter, capability or catalogue entry carries the bell, OSC 9/777 or OSC 2, so which terminal gets which, and the wording, are undeclared. `src/terminal/escapes.ts` emits none of them, so nothing contradicts the design — there is nothing to build *to*. **And the subject may be out of scope**: a reader-declared watch does not exist — `watch(id)` in `src/shell/refresh.ts` is the stall detector's map of streaming entries watched for silence, a homonym — and building one is a run-lifecycle affordance, which the brief puts outside this work |
| **R-COL-006** — Metric polarity drives trend tone | `parked` | **§088 §4 settles it completely and §086 says where it lives.** The three rows are drawn — *val loss ↓ from 0.41, lower is better* / *accuracy ↑ from 0.62, higher is better* / *val loss ↑ from 0.04, … and this is BAD* — with the reason: *a trend that always paints DOWN as ok misleads on half of every ML metric … otherwise the colour is decorative, which this document argues against everywhere else.* §086 homes it: *a metric in a row — sparkline — **inside a table cell***, so the polarity declaration belongs on `ColumnDef` beside `role`, which is the same record R-TBL-004's unit wants and needs no adapter change, since an undeclared column is neutral. The tone follows with no choice left open: the arrow's direction is the delta's sign, its tone is `ok` when the movement runs with the declared-good direction and `error` when against, and neutral takes none. **What blocks the build is the arrow itself**: §088 draws `↑` and `↓` and the registry has no glyph record for either — `sort-asc`/`sort-desc` are `▴`/`▾` and scoped `table-header`, a different domain — so the ASCII rung is a visible choice the design does not make. Parked as 26 |
| **R-EVT-001** — Input windows coalesce by identity | `covered` | C16 I45 · C16 I46 · `src/interaction/router/router.ts` `pointerArm` · `test/unit/router-dispatch.test.ts` T1.100, T1.101, T1.101b — **read as what it is, the rule is a constraint on coalescing rather than a requirement to coalesce**, and both things it forbids are impossible by the arm's shape. `pointerArm` is `{id, epoch}` and the commit is `armed.id === id && armed.epoch === ownerEpoch`, so a sample cannot **change the armed target** — a release elsewhere finds a different id — and cannot **replay against a new owner epoch**, because the counter moved and the comparison fails. T1.101b holds the third: a drag cancels the arm and coming back does not restore it. **No pointer-sample coalescing exists** and its absence cannot violate a constraint on it; what matters is that the constraint is enforced at the commit rather than at the sample, so coalescing added later — in the decoder, or inside a window — arrives already bound by it. **Stated blind spot**: this is coverage of the *safety* clause. If a declared sample window is ever wanted for its own sake, §062's is C03's frame window and the input exemption there is a different rule |
| **R-HON-008** — Configuration values name provenance | `parked` | **the row named the wrong sections: §075 is a complete specification for this and it is a different domain from the one the provenance mechanism serves.** `/config — and the third column is the one that matters` draws the whole thing: a three-column table `key · value · source`, four sources with a tone ladder — *default* muted *nobody chose it*, *config* meta, *env* warn, *flag* error, ordered because *the later it is applied, the louder it is* — and the argument, *a config view without a source column is one you cannot debug: the commonest question is not what is it, it is WHY is it that.* It also draws per-directory precedence, the account's file against this checkout's, *and it wins*. That is stronger than the `/capabilities` and §088 sites this row used to name. **And it is the half with no mechanism.** `src/terminal/capabilities.ts` `CapabilitySource` — `declared | stated | inferred | assumed | unreachable` — with `Answer<T>` returning value and source together is a *capability* provenance, five values about detection, deliberately paired so *a second map from field to source would be the drifting kind*; §075's four are about **configuration** layering, which is `src/shell/config.ts`, where a caller's value meets a framework constant at a `??` that keeps neither. So two halves: the capability half has the record and no reader, and the config half has neither. **What is owed and where the scope line falls**: the display is appearance and the tone ladder is settled, but three of the four sources do not exist — the tree reads no config file, parses no flags, and reads `process.env` only inside `capabilities.ts` by A02's rule — so the subject the display would name has to be built first, and that is configuration plumbing rather than appearance. **Parked as 28**, on the scope line: building config-file loading, flag parsing and environment reading is configuration plumbing, which the brief puts on neither list and which is closest to the out one. The question names the alternative — read the rule as binding where a provenance exists, which makes the **capability** half the live subject, a display over a record that already exists and squarely appearance |
| **R-FOC-001** — Unpainted box focus | `unmet` | **Audited 2026-09-24.** **the button lifts rather than inverts.** `buttonSpans` (`src/presentation/blocks/kinds/simple.ts`) gives every focused button `pick` regardless of `block.tone`, and at 1-bit the bracket rung is identical focused and resting where §017 draws *brackets and bold*; T1.66 checks the resting 1-bit button only. `pills` and `panel` do not answer focus. |
| **R-FOC-002** — Continuous-control focus | `covered` | **Audited 2026-09-24.** C09 I106 · `src/presentation/blocks/kinds/controls.ts` `controlDefinition`, `track`, `isInside` · `test/unit/focus-shapes.test.ts` T1.73. One `focusGround` run over label, gaps, track and value; T1.73 finds the ground's open and close for focused and inside alike, the value's ink unchanged and the row length constant. |
| **R-FOC-003** — Choice-control focus | `covered` | **Audited 2026-09-24.** C09 I105 · `src/presentation/blocks/kinds/controls.ts` `choiceDefinition`, `optionText`, `markOf` · `test/unit/focus-shapes.test.ts` T1.72. Mark and label are one span under the focus style; T1.72 asserts the washed run is exactly `○ linear`, including focused-but-unchosen. |
| **R-FOC-004** — Picture focus | `unmet` | **Audited 2026-09-24.** **framed plots hold; eleven forms and the mosaic do not.** C12 I142 and T2.130 hold the frame. `SILENT` (`test/contract/plot.test.ts`) lists eleven forms that answer no focus, including `smallmultiples` and `pairplot`; `image` does not answer; and a focused mosaic pane paints `focusGround` across its child's rows (`paneGround`, `src/presentation/blocks/kinds/containers.ts`) — across braille, which §018 forbids and figure 5 draws as a heavy border. |
| **R-FOC-005** — Focus is not row-shaped | `covered` | `src/presentation/blocks/types.ts:447` `level` — an element declares `"block" | "row" | "cell"`, so more than one interactive element per row is expressible by construction and the geometry follows the kind. The §017 census measures the consequence: of 23 kinds in `test/support/design-surfaces.ts` `ONE_PER_KIND`, six publish focusable elements and five draw a treatment, and 17 publish none at all — which is why §017's rungs have no subject in this tree rather than an unbuilt one |
| **R-PTR-001** — Focus-first activation | `unmet` | **Audited 2026-09-24.** **direct-action controls need two clicks.** `pointerEffect` (`src/shell/construct.ts`) arms activation only when `onFocused`, for every element but a legend entry; §089 lists link, button, chip, toggle, legend entry and choice as *ONE CLICK acts*. Focus-first holds for rows (T4.64, T4.62). |
| **R-PTR-002** — Remembered focus re-resolves | `covered` | C26 I10 · `src/interaction/router/focus.ts` `resolveFocus` · `test/unit/router-focus.test.ts` T1.3h, T1.3i — the store holds an `ElementAddress`, which is `blockId` plus `elementId` and **never an index**, and `resolveFocus` re-resolves it against the live element list on every read, falling back to the block when the element went and to the start when the block went. **The earlier row asked the wrong question.** It looked for a departure hook — a view that leaves and returns — and there is none, because the tree re-resolves on *every* frame rather than on return, which discharges the rule over a strictly larger set of moments. That the one resolver serves both the render read and the next keystroke is the same file's argument (C26 I8): a fall-forward computed on one side only would leave the store holding a dead address |
| **R-PTR-003** — Hover may preview | `covered` | **Audited 2026-09-24.** C16 I31, I45 · `src/shell/construct.ts` `pointerEffect`, `sampleUnder`, `legendUnder` · `src/interaction/router/router.ts` `observeArming`, `cancelArmOnPress` · `test/unit/session-mouse.test.ts` T4.72, T4.72b, T4.73b · `test/unit/router-dispatch.test.ts` T3.8d. Hover writes only the plot's readout, and only when the sample changes; it never focuses, never toggles a legend, and neither arms nor disarms. |
| **R-PTR-004** — Legend click toggles a series | `covered` | `src/shell/construct.ts:2757` `legendHitAt` — *the toggle itself, shared by the digit key and the legend click* (C16 §4a's legend row, C12 I117), written as **one definition of what a toggle is** so the two cannot disagree. The click reaches the same series state the keyboard does because it reaches the same function |
| **R-PTR-005** — Press arms and release commits | `covered` | **Audited 2026-09-24.** C16 I45, I46 · `src/interaction/router/router.ts` `armPointer`, `commitPointer`, `pointerArm` · `src/shell/construct.ts` `armActivation`, `armId` · `test/unit/router-dispatch.test.ts` T1.100, T1.101b, T1.102 · `test/unit/session-mouse.test.ts` T4.64. The arm is an id, never a cell, and commits at most once; the press alone does nothing. |
| **R-PTR-006** — Unpaired context click is inert | `covered` | C16 I31 · `src/shell/construct.ts` `button0` · `test/unit/session-mouse.test.ts` T4.67 — **the rule is written in the tree's own words, one line above the gate that holds it.** The gesture table returns `null` for anything that is not `button0`, and its comment says why: *recorded rather than absorbed — a second button has no key equal yet, and a meta- or ctrl-modified click has no ⇧↓-shaped state to reach.* The table **is** the declaration of keyboard equivalence — a click is `↓`, click-again is `⏎`, a drag is `⇧↓` — so *no declared keyboard-equivalent action* and *not in the table* are one predicate, and this rule is R-INT-006's parity read from the other side. **Not vacuous**: the decoder really produces `button2` (`test/unit/router-decode.test.ts` feeds `[<2;10;5M`), and T4.67 asserts both halves — `dispatch` returns false **and** the focus is unchanged, *none of the three is a gesture in the table* |
| **R-REF-001** — No leader sequences | `covered` | `src/interaction/router/types.ts` `Binding` — a binding's `key` is **one** chord record, so a leader or a multi-key sequence is not representable in the type. The rule is held by the shape rather than by a check, which is the strongest form coverage takes: there is no state in which it could be violated and no gate that could go quiet |
| **R-REF-002** — Platform chords remain owned | `covered` | **Checked now, as C16 I55** — T1.108 sweeps both profiles for the six chords §063 and §019 name and the measured window-switcher pair, and pins `⌥←`/`⌥→` to word motion; the citation below was `:682` and had drifted onto an unrelated `profile:` line before anyone looked. `src/interaction/router/keymap.ts:722` — `agentPrevious` and `agentNext` take `⌥,` and `⌥.`, which replace `⌥⇥`/`⇧⌥⇥`, *which the OS window switcher takes on Windows and most Linux desktops — the compositor never hands them over*. The rule is followed by a binding that was **changed** rather than by a check, and the reason is on the line so a later edit cannot recover the chord by accident (which is R-KEY-006's clause, met here) |
| **R-REF-003** — Bindings do not change availability | `covered` | C16 I35 · `test/unit/router-keymap.test.ts` T1.35 — *a profile is a condition — one table, and `resolve` refuses the wrong one*. `enhanced-terminal` resolves only where `capabilities.keyboardProtocol === "kitty"`, which is a **capability the terminal reports**, not a state that comes and goes: a chord is either exposed or not for the whole session, so there is no indistinguishable visible state for it to work intermittently under |
| **R-TBL-001** — Table alignment follows value kind | `covered` | C11 I27 · C11 commitment 25 · `src/data/viewmodel/types.ts` `ColumnDef` · `src/presentation/table/kind.ts` `columnAlignments` · `src/presentation/table/cells.ts` · `test/unit/table.test.ts` T1.31, T1.32, T1.33, T1.34 — **built, after the parking that held it was retracted as measured on one of four violators.** `ColumnDef`'s `align` is optional; absent, C11 resolves it from the column's own cells, which is I26's ruling about the decimal point one level up. The classifier already existed as the sort's `kindOf` and moved into that file when it gained a second reader. `numeric` splits on whether any value carries a point — §078 draws both cases in one table, `rows` flush to the inline end and `metric` on the point — `duration` takes `right`, and a column whose values disagree about their kind is text. A **declared** alignment is honoured, so C07's `fallback.ts` keeps its explicit `left` and nothing reaches into an untouched component. The window pins the resolved alignment over the whole table before slicing (I18, I19, F429). **Three things the build found that measuring did not**: `3e-4` did not parse as a number, so §099's own metric column classified as text and could not align — and widening the grammar corrected an ordering defect nobody had reported; the *refinement* claim that `decimal` always equals `right` on integers was false, the column's slack moving from before the number to after it; and right-aligning a `decimal` column's header put the label past every value it names, which moved six goldens before the clause was narrowed. **Stated blind spot**: the derivation moves no golden frame, because every table in that corpus either declares its alignment or holds text — T1.34 goes through `markdownBlocks` for that reason |
| **R-TBL-002** — Decimal alignment | `covered` | C11 I26 · `src/presentation/table/cells.ts:106` `decimalPoints` — `align: "decimal"` is a column kind of its own and the point's cell is computed per column. **The plan's reconciliation table says this is unbuilt and that is now false** |
| **R-TBL-003** — Missing numeric value is unknown | `unmet` | **Audited 2026-09-24.** **a missing numeric cell is a blank.** `rowSpans` (`src/presentation/table/cells.ts`) draws `[]` for an absent cell, and nothing turns it into `—`; a producer that wrote `—` would reclassify the column as text. §078 draws `—` at the point. The ASCII rung is question 13's. |
| **R-TBL-004** — Numeric grouping and units | `covered` | C11 I28 · C11 commitment 26 · `src/presentation/table/kind.ts` `groupingColumns`, `grouped` · `src/presentation/table/cells.ts` `decimalPoints` · `test/unit/table.test.ts` T1.35, T1.36, T1.37 · `test/contract/table-decimal.test.ts` T2.14 — **both clauses discharged, by different means.** *The unit* is satisfied by shape rather than by code: §078 states it as a constraint on **where** the unit lives — *a units row in the header is a row you have to look back at* — the unit travels in the cell value, `src/presentation/table/sort.ts` `asDuration` parses `12m`, `1h 12m` and `2h ago` back out of it, and no units row exists anywhere in the render. The row that said a `ColumnDef` unit field was missing had the remedy backwards. *The grouping* is built: three all-or-nothing clauses over the column, because a column grouping some rows and not others puts `1,204` above `41208`. **§099's own figure moved and that is a ruling**: it draws `steps 1284` ungrouped and cites neither this rule nor R-TBL-001, §078 is the sole example for both, and its fourth rule is unconditional — so the rule outranks a figure drawn to demonstrate the decimal point, whose property survives intact since `1,284` still ends where `0.941`'s point sits. Six goldens updated, named before the run. **Stated blind spot**: clause 1's only live subject is a duration — dropping it killed no test, because a text column never arrives — and `10000s` staying bare is the conservative reading of a case the design draws nowhere |
| **R-TBL-005** — Column-local tail truncation | `unmet` | **Audited 2026-09-24.** **any column may truncate, numbers included.** `planColumns` (`src/presentation/table/plan.ts`) never reads content, and `truncateParts` applies to every cell wider than its plan, so a bare `41208` is cut in a four-cell column; §078 rule 5 says the widest text column yields first and a number column never truncates. T2.15 cites this rule and tests decimal points. |
| **R-TBL-006** — Sorting belongs to the header | `covered` | `src/presentation/table/cells.ts:191` — the header row carries `sortAsc`/`sortDesc` on the active column, so the sort state is drawn on the header and nowhere else |
| **R-KEY-003** — Conventional key vocabulary | `unmet` | **Audited 2026-09-24.** **⇧↑↓ and esc are unbound at a prompt that can hold several lines.** `insertNewline` (`src/shell/keys.ts`) makes a multi-line prompt, and `selection.up`/`down` and `escape` have no `prompt` row in `src/interaction/router/keymap.ts`; T1.37 excuses the arrows as *the prompt is one line*, and its capture arms are prose the loop skips. |
| **R-KEY-004** — Bindings remain discoverable | `unmet` | **Audited 2026-09-24.** **the scope line names the prompt's keys everywhere.** `OwnerRung` has one `scope` member for prompt, transcript and block, and `ownerChips` (`src/shell/chrome.ts`) draws `⏎ send` with an entry focused; §015 draws `esc back` and the focused primary action. Raised rungs lead with their owner and keep their exit when shed (T1.46e). |
| **R-PRI-002** — Primitive definitions stay locatable | `covered` | `docs/design/language/build-calcium.mjs:415` — `if (!primitive.ruleIds.includes('R-PRI-002')) throw`, plus the check above it that every `definedIn` key names a section that exists. A primitive pointing at a missing canonical section fails the build, so *locatable* is a property of the commit rather than of a reader's diligence |
| **R-PRG-002** — Bar alphabets declare placement | `parked` | **Audited 2026-09-24.** **parked as 32, both clauses.** *Discrete steps use posts*: §033 draws posts and §035–§036 draw counted work in slant, which is what `granularity: "segmented"` selects. *Sub-cell progress uses braille*: the registry's braille bar is whole-cell. What holds — a bar needs a known total, and ASCII is the universal fallback (T2.158). |
| **R-MOT-012** — Ramp meaning precedes appearance | `unmet` | **Audited 2026-09-24.** **direction is checked for streaming, not for progress.** The band sits at the head (T1.54) and typewriter reveals from the start (T2.117e), but for shimmer, wave, sweepbar, chase, wipe, sweep and marquee T2.117e (`test/contract/ramp-registry.test.ts`) asserts only that they vary — a reversed shimmer passes. Thirteen ramps' attention groups are parked as 31. |
| **R-KEY-005** — Help is a durable generated entry | `covered` | C16 I57 · C23 I79 · `src/shell/execution.ts` `emitLocal` · `src/shell/keys.ts` `helpKeymap` · `test/contract/surface.test.ts` T1.109 · T1.100, T1.98. **Built 2026-09-24.** F1 and `?` submitted `/help keys`, so F1 mid-sentence cleared the draft and recorded a line never typed; they now run the same handler and append with no settle. T1.109 drives `?` from a focused entry, the first row to order the listing from a rung other than `prompt`. Its control types `/help`, because C20 I4 collapses a repeated line and a control typing `/help keys` hid a mutation that recorded it. *Appears during a run* holds by construction — `emitLocal` never reads the guard — and no row drives it. |
| **R-KEY-006** — Key choices retain their rationale | `covered` | `docs/KEYS.md` — generated from `defaultKeymap`, with `test/unit/keymap-table.test.ts` failing when the file and the live keymap disagree, so the record cannot drift from the bindings. It carries both halves the rule asks for: the **convention** (*every action with an `enhanced-terminal` route also has a `both` route, because ⌘, ⇧⏎, ⌃⇧-letters and ⌃⇥ are byte-identical to their unmodified forms without the protocol*) and the **departure** (*every `⌥` route needs the terminal to send Option as Meta*), each with its reason and its history |
| **R-GLY-002** — Reservations are verified in the composed grid | `unmet` | **Audited 2026-09-24.** **the measurement runs only in a browser.** The `generated-glyph-grid-check` script is emitted into the HTML (`docs/design/language/build-calcium.mjs`) and nothing executes it; `check-calcium.mjs` asserts the script's text, and the builder compares `reservedCells` with a declared width. And SS64 is cited for this rule (C10 §4k.4, `tools/enforce/index.mjs`) while measuring collisions, not widths. Owed: a tree-side row measuring `cells()` of every current glyph and delimiter in both forms against `reservedCells`. |
| **R-KEY-007** — Generated action-map surfaces | `unmet` | **Audited 2026-09-24.** **two rows hand-write registry chords.** `moveSemanticCaretUp`/`Down` at `semanticSelection` (`src/interaction/router/keymap.ts`) write `{ name: "up" }`/`"down"` where the registry names `move.up`/`move.down` — C16 I42's *written once, and there*. T1.97 pins the row set, not literal against registry. |
| **R-REG-002** — Normative authority is explicit | `covered` | **Audited 2026-09-24.** `docs/design/language/check-calcium.mjs` `checkStructuredCoverageAndRetention`, `checkGeneration` · `docs/design/language/build-calcium.mjs` `reconcileSectionBlockIdentities`, `blockContentDigest`, `assertCurrentCitations` · `make design-check`. No section keeps opaque HTML; every block is `example` with a recomputed digest; every section carries `data-rule-ids` and the builder throws on a governing citation that is not current. |
| **R-SEL-001** — Two selections, and the mouse belongs to the emulator | `unmet` | **Audited 2026-09-24.** **no copy affordance appears on focus.** No block kind draws one and `ownerChips` (`src/shell/chrome.ts`) has no arm for a focused block; the design's footers draw `⌃⇧C copy the values` on a focused row. The terminal owns the mouse (T4.30) and copy takes the source (T1.42, T1.41h). |
| **R-SEL-002** — The render must survive a naive drag | `parked` | **Audited 2026-09-24.** **parked as 6, for clause 2 only; the rest is held.** *A bounded block's content does not sit inside vertical rules* contradicts §021, §045, §047, §048 and §096. Continuation rows carry no gutter decoration (C09 I87, `test/unit/naive-drag.test.ts` T2.154); the scrollbar is the last column (T1.26). C09 §7b's *there is no scrollbar in `src/`* is stale. |
| **R-SEL-003** — A block is atomic in a selection | `unmet` | **Audited 2026-09-24.** **the ground may stop at the first inner reset.** `washRow` (`src/shell/paint.ts`) wraps a finished line whose styled spans each end in `SGR_RESET`, so on a bordered or toned first row the wash would stop at the first run — by reading; T1.40b tests an unstyled string. Taken whole and crossing blocks are held (`blocksTouched`, T1.36, T1.38b). |
| **R-SEL-004** — A block's copy form, per kind | `unmet` | **Audited 2026-09-24.** **an image copies its alt and not its path.** `copy: (block) => block.alt` (`src/presentation/blocks/kinds/image.ts`), and `Image` carries no path — the builder reads the file and drops it. Table, patch, plot and scroll forms are held (T1.41h, T1.41i). |
| **R-SEL-005** — esc clears the selection; a second esc leaves the rung | `unmet` | **Audited 2026-09-24.** **the footer always says the press leaves.** `ownerChips` (`src/shell/chrome.ts`) takes no selection state and draws `esc out`, so the first press — which clears — is labelled as leaving. The two presses are held (C16 I51, T1.41b, T1.41c). |
| **R-SEL-006** — Selection owns the ground, focus keeps its mark | `unmet` | **Audited 2026-09-24.** **selection over a diff has no subject.** `patch` declares no elements (T2.48), and in copy mode `washRow` (`src/shell/paint.ts`) cannot override an embedded ground; T2.45 is `it.todo`. The four element states and the 1-bit inverse are held (T1.23, T1.40b). |
| **R-SEL-007** — Rectangular copies cells, and says so | `parked` | **Audited 2026-09-24.** **parked as 5, and it blocks the whole rule.** The registry has no rectangular action and no binding, so `rectBetween` and `cellTextOf` (`src/shell/semantic-selection.ts`) have no caller; the clip and the escape-free cells are built (C14 I42, I43). |
| **R-SEL-008** — a, A, and no whole-record key | `unmet` | **Audited 2026-09-24.** **A does not say what it did.** `#selectEntries` (`src/shell/session.ts`) updates the state and commits a frame; the `construct.ts` doc comment says *A says what it did*. `a`, `A` and no whole-record key are held (T1.41d, T1.97). |
| **R-SEL-009** — Three things redraw while frozen | `unmet` | **Audited 2026-09-24.** **the count is never drawn.** `semanticSelectionCount` is supplied (`src/shell/construct.ts`) and read by nothing; and the `copy` chip is shared by both modes (question 4), advertising `↑↓ extend` in native mode. Freeze and stopped spinners are held (T1.30, T4.34). |
| **R-SEL-010** — Buffered arrivals are announced on exit | `unmet` | **Audited 2026-09-24.** **the waiting chip is built and checked by nothing.** `ownerChips` (`src/shell/chrome.ts`) draws `N waiting` while content is held; no row asserts it. Buffering and the landing on exit are held (C14 I31, I34, T1.34). |
| **R-SEL-011** — No clipboard is stated, never silent | `unmet` | **Audited 2026-09-24.** **no clipboard leaves the process.** No OSC 52 in `src/terminal/escapes.ts`, no OS clipboard, and no statement when none is available; `#copySelectedEntries` (`src/shell/session.ts`) writes the kill buffer, while a `construct.ts` comment claims the refusal exists. |
| **R-SEL-012** — A drag scrolls the container the anchor is in | `unmet` | **Audited 2026-09-24.** **the keyboard caret does not scroll.** `#moveSemanticCaret` (`src/shell/session.ts`) moves the caret and scrolls nothing, so a caret stepped past the window leaves the viewport — C14 I32 *the caret pulls the viewport*, unbuilt. The drag's container and the wheel's are held (T1.44, T4.37, T1.105). |
| **R-SEL-013** — Three autoscroll bands, and it stops at the container's end | `unmet` | **Audited 2026-09-24; the esc half built the same day.** `esc` and `⌃c` now end the gesture and its ticker (C14 I48, `#endDrag` in `src/shell/session.ts`, T4.37b in `test/integration/copy-drag.test.ts`, run `c14-drag-gesture` 2/2) — measured before, a held pointer kept scrolling after esc. **Still unmet, by reading:** no horizontal autoscroll (`#armAutoscroll` tracks a row only); a tick scrolls without extending the selection; and `extendTo` (`src/shell/semantic-selection.ts`) has no container clamp. The three bands and the release are held (T1.45, T4.37). |
| **R-SEL-014** — A container you pass through is taken whole and does not scroll | `unmet` | **Audited 2026-09-24.** **the code holds and two clauses are unchecked.** T1.46 (`test/unit/copy-drag.test.ts`) promises the box's offset unchanged and the copy whole, and asserts neither; no row asserts a nested child is absent from the span set. Whole and all-rows are held (T1.46, T2.35b). |
| **R-SEL-015** — All-or-none applies continuously | `unmet` | **Audited 2026-09-24; the return half built the same day.** ⏎ is now `y`'s `copySelectedEntries` at `semanticSelection` (C14 I47, T1.47 in `test/unit/router-keymap.test.ts`), so the footer's `⏎ copy` is bound. **Still unmet:** the drag path (`extendTo`, `placeCaret` in `src/shell/semantic-selection.ts`) has no row that walks a drag down past a block and back above it, and the count is drawn nowhere (R-SEL-009's row). The keyboard's re-derivation is held (T1.38b, T1.39). |
| **R-THM-002** — A high-contrast theme keeps 7 : 1, and says so | `unmet` | **Audited 2026-09-24.** **the 7 : 1 floor covers three grounds of the six an hc theme paints text on.** `validateHighContrast` (`src/presentation/theme/contrast.ts`) applies `floor` over `textSurfaces` only; diff grounds use `floorFor` (4.5) and the prompt chip's `bgDeep` is checked by nothing. Measured by hand: hcDark on `diffAdd` 4.77–4.82, hcLight on `diffRemove` 5.73–6.68, `tone.meta` on `bgDeep` 6.38 / 6.51. The docblock at the rule's name in `contrast.ts` has no function under it. |
| **R-THM-003** — A high-contrast theme takes focus and selection as bands | `unmet` | **Audited 2026-09-24.** **state moves to the glyph per terminal, not per cell.** `headMark` (`src/presentation/blocks/glyphs.ts`) returns `●` whenever `toneCarries(caps)`, and its call site passes capabilities rather than the ground — so a focused call head in hcDark or hcLight draws `●` in the band's one ink for every state. C10 I45 states the per-cell rule in prose. Owed: the banded arm resolving `CALL_STATE_GLYPH`, and a row over five states on a band. |
| **R-THM-004** — A floor's scope is every ground the theme paints text on | `unmet` | **Audited 2026-09-24.** **the scope is a written list, and the list's premise is false.** `textSurfaces` (`src/presentation/theme/contrast.ts`) is bg, bgElev, focusGround, excluding `bgDeep` because *no text lands on it* — and the prompt chip paints `tone.meta` on `surface.bgDeep` (`src/shell/paint.ts`). T2.44 pins the list by equality. Owed: the scope derived from the grounds renderers paint text on. |
| **R-STA-004** — Availability ranks above a semantic extent | `unmet` | **Audited 2026-09-24.** **availability has no subject.** No block carries `disabled` (`src/data/viewmodel/types.ts`), so the well that outranks an error ground is unbuilt; T2.47 and T6.105 are `it.todo` (`test/contract/theme.test.ts`). Freshness is C04 I127's stale reading, landed with R-HON-002. |

---

**No total is stated here as prose.** The four figures above are the tool's
output and go stale the moment a row moves; to count them is to run it:

    node tools/rule-status.mjs

