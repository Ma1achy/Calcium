# The rule ledger — every `current` rule, resolved against the tree

**Why this file exists, and it is the eighth blind spot arriving at this
reconciliation's own record.** The sixteen MRs were checked, MR by MR, against
**the plan's** text — the deliverables each one names. The plan is not the
registry. `AUTHORITY.md` puts `status: current` at the top of the precedence
ladder and the plan nowhere on it, so a rule the plan never mentioned could be
unbuilt, uncited and invisible to every per-MR report, and forty-one of them
were. Measured:

    118 current rules in docs/design/language/calcium-registry.json
     70 named somewhere outside docs/design/language/   (cited)
      7 discharged under another name, with the subject  (covered)
     41 resolved by nothing yet                          (owed)

**A count in prose is a snapshot with no mechanism** (F142), which is the
argument for `tools/rule-status.mjs` rather than for this table alone. The tool
re-derives all four figures and fails when any row's claim stops holding.

## The three states, and why `covered` is separate from `cited`

- **`cited`** — the rule's `R-XXX-NNN` appears in `src/`, `test/`, `tools/`,
  `docs/components/`, `docs/architecture/`, `docs/design/layout/`,
  `PARKED_QUESTIONS.md` or `CALCIUM_ROADMAP.md`. The R-ID is the resolution and
  nothing more is claimed here: **cited is not the same as satisfied**, and this
  file does not pretend otherwise. What it rules out is a rule nothing in the
  repository has ever looked at.
- **`covered`** — the rule is discharged by a mechanism that predates it and
  carries another name, so no R-ID appears and none should be added merely to
  make a table green. The row names the subject, and the tool resolves it: every
  path must exist and every identifier must appear in a file the row cites,
  which is `roadmap-status.mjs`'s check and the one that caught a row naming a
  real mechanism in the wrong file.
- **`owed`** — resolved by nothing yet. **This is a remainder, not a
  divergence.** Nothing here is recorded as knowingly divergent; each is work
  the reconciliation has not reached, and the row is what makes it countable
  instead of invisible.

## The check that keeps this honest

An `owed` row whose rule **has** since been cited is a failure, not a pass. A
ledger that only checked its positive claims would certify whichever subset
chose to carry one, and the stale half is the half that reads as coverage —
`roadmap-status.mjs` §2's argument, in the other direction. So the three sets
partition the 118 **by equality**, both ways, and a rule that lands fails here
on the day it lands until its row moves.

**What this file does not check**, stated rather than left as an omission: a
`cited` row proves a reference, not a behaviour. The instrument that asks
whether a rule is *satisfied* is the carrier matrix, the golden frames and the
mutation runs — this one asks the prior question, which nothing asked before.

---

| rule | state | resolved by |
|---|---|---|
| **R-SPC-001** — Registry authority | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-COR-001** — Three histories | `covered` | C13 I20 · `src/shell/transcript-persist.ts` `PersistFs` · `src/viewport/transcript/cap.ts` `MARKER_ID`, `countBlocks` · `test/unit/transcript-persist.test.ts` T1.28 · `test/unit/block-cap.test.ts` T3.22 — **the earlier row said the scope question had to be answered first and never asked it.** Asked: two of the three histories are this repository's and both are built, and the third is the application's by the design's own sentence. *The durable event record* is the resume file, and its header states the property the rule needs — **only settled entries are written, which makes the file append-only in fact rather than by assumption**. *The rendered window* is the store under its block cap, and eviction leaves `transcript:evicted` above what survives, which T3.22 asserts with two caps and two markers. **And the two cannot touch**: nothing in the persistence module names eviction, the sweep or the marker, so *eviction changes the window, and neither erases history* holds by the file having no code path that could shrink it. *The model context* is out of this repository's authority — §065's own words are **only a harness holds the turns to say it**, and compaction, turn counts and token budgets are the harness's throughout |
| **R-COR-002** — Render does not seize ownership | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-COR-003** — Independent carriers | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-STA-001** — Orthogonal state axes | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-STA-002** — One ground per cell | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-STA-003** — Hover is not focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-OWN-001** — Owner routing verdicts | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-OWN-002** — Owner epochs | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-OWN-003** — Pointer commit | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-QST-001** — Question form and placement | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-QST-002** — Inspection suspends | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-QST-003** — Editor state isolation | `owed` | **one owner exists, so the isolation has nothing to be wrong about.** `createEditor` is called once (`src/shell/construct.ts:1128`) and the prompt is its only owner. The second owner is M15's typed reply, which is what makes *isolated by owner* falsifiable — A03 §2's vacuity class, and the honest reading is that this is unbuilt rather than satisfied |
| **R-QST-004** — Single approval overflow path | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-001** — Motion preference | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-002** — Spinner timing | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-003** — Reduced-motion mapping | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-004** — Semantic frame contrast | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-005** — One-cell animation carriers | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PRG-001** — Meter semantics | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-TXT-001** — Text coordinate systems | `covered` | `src/interaction/editor/graphemes.ts` `sliceBetween` · `src/interaction/parser/classify.ts` `tokens` · `src/presentation/text.ts` `cells` — the four systems are four modules and the editor's own header says so: *`graphemes` takes text apart, `words` classifies it, `layout` places it on* cells. Editing goes through `sliceBetween`/`splitAt`, which are grapheme-indexed, so a public span cannot land inside a cluster |
| **R-GLY-003** — Glyph registry and reserved cells, which bind in fixed columns | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-CAP-001** — Capability vector | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-ACC-001** — Linear semantic rendering | `owed` | **one of §107's nine fields is built and it is the id.** The element record is `{id, level, rows, cols}` (`src/presentation/blocks/types.ts`) — a stable id and a rectangle, where the design's semantic node wants *id, role, name, description, value, state, position, actions, relations*, and `level: "block"|"row"|"cell"` is a geometry hint rather than a rôle. The two renderings — `rich` and `linear`, the second an append-only stream selectable by `--linear` or `CALCIUM_RENDER_MODE` — do not exist, and neither does the announcement policy (`none`/`polite`/`assertive`, deduplicated and rate-limited, *from semantic events, never from diffing the screen*). **One §107 sentence is already held**: *if a patch removes the focused node, focus re-resolves by the declared stable-id fallback* is R-PTR-002's `resolveFocus`, covered above |
| **R-TRU-001** — Terminal control boundary | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-THM-001** — Theme tokens | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PRI-001** — Primitive index | `covered` | `docs/design/language/build-calcium.mjs:412` — the builder throws when a primitive lacks `definedIn`, its canonical definition links, and `make design-check` runs it. The index is generated from the registered primitives rather than written beside them, which is the rule enforcing itself in the artefact it is about |
| **R-TAB-001** — Component state tables | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PER-001** — Permission posture registry | `owed` | **§088 draws the display this rule is about, and it is a surface the tree has no equivalent of.** `/permissions` is a table — *what · where · granted* — with rows like `rg --stats -uu · the count agent · 22:41` and a key line `↑↓ move   ⌥⌫ revoke   ⌃⇧C copy`, and the argument on the fixture is the rule's own: *always allow is a decision, and a decision you cannot see is one you cannot revoke*. So *explicit, inspectable and revocable* are three separate claims and the tree satisfies none: nothing stores a standing grant, nothing lists one, nothing revokes one. **And it stays blocked on parked 18 rather than merely unbuilt** — the posture vocabulary is the two normative sources' disagreement, so a registry built now would have to choose between `skip` and `ask` to name its rows |
| **R-STR-001** — Call shape | `covered` | `src/shell/documents.ts` `callHead` and `callBody` — the head is one block, the gutter is `src/presentation/blocks/kinds/simple.ts` `glyphLead`'s two cells, and `callBody` emits the settled `notice` plus **at most one** `scroll`, guarded by `call.output !== undefined && call.output.length > 0`. *At most one bounded body when output exists* is that condition |
| **R-STR-002** — Sibling spacing | `covered` | C09 I17 · C22 I85 · `src/shell/builders/index.ts` `gapBefore` · `src/shell/entry-layout.ts` `ENTRY_GAP` · `test/unit/frame-budget.test.ts` T1.45 — **both halves are built and the earlier row searched the wrong files.** It looked in the shell's document and construction files and concluded the transcript inserts no row; the half it missed is `ENTRY_GAP = 1`, *the blank row that closes every entry* — **the entry's own**, so C14 measures it with the entry rather than the loop inserting it between two, which is why a search of the loop found nothing. T1.45 asserts it measured, drawn, windowed and element-free. *Spacing inside a body belongs to that block* is the other half: `finish` resolves `gapBefore` to `padding: {t: 1}`, with the block marked when the value was a default and left alone when explicit, so `b.seq` touches only the blocks that were never asked |
| **R-STR-003** — Width-bounded nesting | `cited` | **the figure was checked and the design does not draw it, which parks the first clause as 25.** Over all 130 fixtures, every `⎿` row against the head above it: **28 instances at a two-column step and none at three**; the nine remaining are step 0, where the line above is the `✦` activity line and is not a parent. `GLYPH_INDENT`/`prefixCells` (`src/presentation/blocks/kinds/simple.ts`) give a head two cells and `⎿` four, so the tree draws the same two-column step the kit's own frames do — by arithmetic, since `prefixCells` *is* the hanging indent. The contradiction is 28 figures against one sentence and amending a `current` rule's text is not mine to take. The second clause — nesting limited by width while the deepest child clears its minimum — is untouched by the answer and stays owed on its own account |
| **R-STR-004** — Inline-end breathing cell | `covered` | C22 I109 · `CONTENT_MARGIN_R` — `region.width = max(1, size.columns - 1)`, with the three exemptions each already an invariant (C22 I81, C22 I86, C22 I87) |
| **R-COL-001** — Red semantics | `owed` | **four clauses, three of which resolve and one blocked on parked 18.** *Red is never the only carrier* is C10 I56's carrier table, whose test of independence is the 1-bit rung. *Removal* keeps its role in C25's patch renderer and *identity* in the hue palette, which C10 I55 declares `carries: "decoration"` — a hue is not a status. **The unresolved clause is *override red means unsafe override***: the permission postures it belongs to are the two normative sources' disagreement parked as question 18, so the second red has no slot to be declared in yet |
| **R-COL-002** — Regional tone budget | `owed` | **two-thirds is built under another name and the row that said *nothing names it* was wrong.** `PaletteSpec.carries: "meaning" | "decoration"` (`src/presentation/theme/types.ts`) is the regional mechanism, and `tokens-dark.ts` declares four palettes beside each other: `tone` (10 slots, `meaning`), `syntax` (9, `meaning`), `categorical` (8, `decoration`), `spectrum` (9, `decoration`). *Code carries its own adjacent palette* is `syntax` and *plots* is `categorical`, both shipped. **Patches took a different shape**: `diffAdd`/`diffRemove` are **surfaces**, grounds on the row, not an indexed ink palette — so the third region is answered by a ground rather than by a palette, and whether the design wants a palette there is not settled by the rule's text. **The clause with no subject is the budget itself**: *an entry has three tones* is a count, and nothing counts. `tone` offers ten slots and no site limits what one entry spends of them |
| **R-COL-003** — Ground and mark | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-COL-004** — Ground redundancy | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-COL-005** — Shape-specific focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-006** — Semantic motion | `covered` | C09 I99 · `src/presentation/blocks/ramp.ts` `AMBIENT_ANIMATIONS`, `effectiveAnimation` · `test/contract/spinners.test.ts` T2.168 — **the earlier row said nothing names it and that was wrong.** The attention group *is* what an animation says: `rampPolicy.attentionGroups` splits the ramps into `ambient` — `glint`, `drift`, `tide`, the three the design calls decorative — and `working`, `waiting`, `attention`, `terminal`, *each of which says something a reader is meant to act on*. `reduced` stops exactly the ambient three and keeps the other four, which is the rule's two halves made operational: motion that only says *look here* is the group a preference can silence, and motion that says what is happening is the group it cannot. The set is the registry's own `attentionGroups` and T2.168 compares the two **by equality**, so a fifth arriving in the registry fails rather than passing unseen |
| **R-MOT-007** — Duration-independent shape | `covered` | C09 I8 · `src/presentation/blocks/kinds/containers.ts:135` — *`measure` never sees the tick*, so a block's geometry cannot vary with elapsed time. The call's shape is its measured rows, and the duration reaches only the spinner and the tone |
| **R-MOT-008** — Agent bloom ownership | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-INT-001** — Scopes are visible | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-INT-002** — No typing-scope single keys | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-INT-003** — Scroll is not focus | `covered` | C26 §7a · `src/shell/construct.ts` `ScrollOffsets` — the offset is view state of its own, so a scroll writes `ScrollOffsets` and never the focus address; §7a carries the sentence in its heading |
| **R-INT-004** — Minimum focus reveal | `covered` | C26 §7a · `src/shell/pull.ts` `pullIntoView` — the file's own opening line is *FOCUS PULLS THE VIEWPORT, BY THE MINIMUM, one row for one row*, and `pullIntoView(held, from, to, window)` is the minimum computed as one number |
| **R-INT-005** — Continuous control commit | `owed` | **the chain is three links and all three are open** (C26 §8b.8.2b, measured). *The declaration*: no field says an element has an inside — `PlacedElement` is `{id, level, rows, cols}`. *The entry*: **nothing in `src/` ever calls `setMode("interact")`** — the one caller is the router's `⌃c`, with `"navigate"`. *The bridge*: `FocusState.inside` is read by `isInside` in `src/presentation/blocks/kinds/controls.ts` and written by nothing, the only constructions anywhere being two test files. So §018's third state is drawn, specified and has never appeared in a frame, and the rule is satisfied by there being no commit from **anywhere** rather than by refusing one from outside — A03 §2's class. The earlier row's *half built* was the renderer's half alone, and it read the absence of the other two as a router gap rather than as three. **The bridge was written and reverted**: with the two links above it missing, no test can reach the state it writes, and a harness that constructs one nothing else can is the form that reads as coverage |
| **R-INT-006** — Pointer parity | `covered` | `src/shell/construct.ts:2962` — the pointer's gesture table onto the key effects (C16 §4a, C16 I31), and its header states the rule: *nothing here can store a location the keyboard could not*. Every arm lands through the same `focus` calls and the same `keys.table` effects — a click is `↓`/`tab`'s state, click-again is `⏎`, a drag is `⇧↓`, the wheel inside a box is `PgDn` on it — so parity is **structural rather than remembered** and there is no second focus mechanism to drift |
| **R-INT-007** — Escape owner path | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-INT-008** — No unsolicited reader motion | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-HON-001** — Dash is unknown | `covered` | `src/presentation/plot/ramp.ts` `absent` — the ramp alphabet carries `absent` as a member beside `empty`, `—` U+2014 dropping to ASCII `-`, with the reason on the line: *there is no braille glyph that reads as nothing was reported*. An unreported value and a value of zero take different marks by construction |
| **R-HON-002** — Staleness timestamp | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-HON-003** — Bounded-window residue | `covered` | C04 I49 · `src/presentation/blocks/kinds/containers.ts` `residueRow` — *a bounded window says what it dropped* is the residue row, `⋯ N above, M below` / `⋯ +N more`, measured at `height+1` independent of the offset |
| **R-HON-004** — Refusal reason | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-HON-005** — Numbers have units | `covered` | `src/presentation/blocks/kinds/status.ts` `elapsed`, `countdown` · `src/shell/chrome.ts` `formatFrameCost` · `test/edge/status.test.ts` T3.42a — **the earlier row stopped at a homonym and never counted the population.** *unitsOf* really is a table's divisible units and not a number's; what it did not ask is how many numbers the framework draws at all. **Three, and all three attach the unit**: `elapsed` returns `4s` or `2m 30s`, `countdown` is drawn as *retrying in 12s*, and `formatFrameCost` returns *last 9.6ms* with its reason on the line — *one decimal, and the unit attached*. T3.42a asserts the drawn row as `loading (4s)`, so the unit travels into the frame rather than stopping at the formatter. **The residue is a table cell and it is already held elsewhere**: a number arriving from the far side has no unit to travel with it, because ColumnDef carries no unit field — which is R-TBL-004's second clause and is owed there, not twice. A count the framework draws bare — `⋯ +5 more`, `⋯ 3 above, 7 below` — is not a violation: the design draws the same figure the same way in §095 |
| **R-HON-006** — Copy source | `covered` | `src/shell/semantic-selection.ts` `copyTextOf` — it takes the entries' `Block`s and hands them to `copySequence`, the per-kind copy form (R-SEL-004). The frame is never a parameter, so *the source, never the rendering* is a property of the signature rather than of a rule the copy path follows |
| **R-HON-007** — Histories stay distinct | `covered` | C13 I20 · C14 I24 · `src/viewport/transcript/cap.ts` `MARKER_ID` · `test/unit/block-cap.test.ts` T3.22 — R-COR-001's subject from the honesty side, and the marker is the mechanism: the window shrinking is **said** rather than silent, so the visible window cannot pass itself off as the record. T3.22 is the sharp case — *two caps, two markers*, the block cap's notice above and the row cap's beneath — which is two windows each declaring its own loss rather than one summary standing for both. The model context is the harness's here as it is on R-COR-001, so *never masquerade as one* has two subjects in this repository and both declare themselves |
| **R-DEG-001** — One-bit survival | `covered` | C10 I56 · §4k.5's carrier table — *every distinction survives to one-bit* is the table's own test of independence and its only one: `mark`, `word`, `weight`, `position` and `inverse` survive the rung, `tone` and `ground` do not, so a pair drawn from the second set is one carrier written twice. Twelve rows, each traced to a field and a renderer, held to the registry's axes by equality (C10 T2.57) |
| **R-DEG-002** — Capabilities enhance | `covered` | `test/unit/router-keymap.test.ts` T1.36 (keys) · A03 SS65 (glyphs) — *a capability makes things better, never possible* in its two halves. T1.36's own words are *the enhanced rows are additions, never the only way*, over every action in the table; SS65 holds every `current` registry glyph to a mark this tree can draw, and 16 of the 17 declare an ASCII fallback. **The seventeenth is the rule kept rather than broken**: `work-unit` carries `asciiResolution: "state"` with its reason — above 1-bit the head mark is one glyph and tone carries the state, at ASCII the state carries itself, and pinning `o` would have spent the mark `choice-open` needs |
| **R-DEG-003** — Set-level width check | `covered` | C09 I93 · `test/unit/blocks.test.ts` T1.61 — the row is named *the set degrades whole, and the check is on the set*; `scrollbarSet` returns one whole set or the other, so no member can take an ASCII rung alone |
| **R-INT-009** — Immediate feedback | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-INT-010** — Reversible action | `covered` | `src/shell/chrome.ts` `shedToWidth` — the footer sheds from the right and keeps **two** chips at every width: chip 0, the owner, and the way out, found by name (`esc`, `host escape`). So the safe exit survives the narrowest responsive rung by construction rather than by a caller remembering to draw it, and where a rung has no escape the second survivor is its primary action — §103's order read literally |
| **R-KEY-002** — Repeat is scoped and declared | `cited` | **built** — C16 I53 · commitment 39 · `src/interaction/router/repeat.ts` `repeatSteps`, `repeatFor` · `test/unit/router-keymap.test.ts` T1.159–T1.159e. Four of §020's five rows have a subject in this keymap and carry their declared numbers, compared by equality; the fifth is a slider, which the keymap has no action for, so no policy names one. The degradation rung is **structural rather than checked**: without the kitty protocol no event carries `repeat`, so the arm is unreachable and the OS's own rate stands — the absence of the field is the absence of the capability. *Nothing is synthesised* is held by T1.159e as a source assertion, and a hand mutation adding a scheduler left **37 of 38 rows green** with only that one red, which is the argument for asserting it structurally. **§020**§020 specifies it completely, with every number, and the half the plan called missing is already built.** The plan said the two-profile keymap needed *one bit in the decoder*; that bit is there — `KITTY_EVENT` maps `:1` press, `:2` repeat, `:3` release, `Key.event` carries it, and `src/interaction/router/router.ts` already consumes it for the `held` set that keeps a key held across a question's arrival from answering it. **What is absent is every timer.** §020 gives DAS/ARR *per binding, because they are not one question* — `↑↓` in a list 170/25, `⌥↑ ⌥↓` page 250/90, a 3D orbit 0/16, a slider 200/40, `⌫` in the prompt 300/30 — plus the acceleration ladder (1 step per repeat held 0–1 s, 4 per repeat to 3 s, 16 beyond, *so a 10,000-row table is crossable by holding an arrow and a 2-row one is not overshot*) and the degradation rung: without the protocol, the OS's repeat unchanged, *and the capability row says so rather than the app pretending*. No binding declares either number and the tree passes the OS's repeats straight through. **The seam this needs is a clock in the router**, which A02 forbids reading and `shell/session.ts` already injects, so it is a hand-down rather than a new source |
| **R-MOT-009** — Ping-pong endpoints occur once | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-010** — ASCII motion preserves semantics | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-011** — Shared alphabets share timing | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-NTF-001** — Watched completion earns notification | `cited` | **parked as 27, on three counts, and the row that called this *the clearest unbuilt feature with no visible choice open* was wrong on the last part.** It is the **only** `current` rule in the registry about watching, and everything that would give it a subject is `status: example`, which `AUTHORITY.md` says does not bind. **The earning table exists twice and they differ**: §014 has four rows including *the model failed — always*; §088 §3 revises to five, adding *a watched run ended* and *a watched run failed* as **new**, and drops *the model failed* without saying so — a decision and an omission read identically. **The three rungs have no record**: no glyph, delimiter, capability or catalogue entry carries the bell, OSC 9/777 or OSC 2, so which terminal gets which, and the wording, are undeclared. `src/terminal/escapes.ts` emits none of them, so nothing contradicts the design — there is nothing to build *to*. **And the subject may be out of scope**: a reader-declared watch does not exist — `watch(id)` in `src/shell/refresh.ts` is the stall detector's map of streaming entries watched for silence, a homonym — and building one is a run-lifecycle affordance, which the brief puts outside this work |
| **R-COL-006** — Metric polarity drives trend tone | `cited` | **§088 §4 settles it completely and §086 says where it lives.** The three rows are drawn — *val loss ↓ from 0.41, lower is better* / *accuracy ↑ from 0.62, higher is better* / *val loss ↑ from 0.04, … and this is BAD* — with the reason: *a trend that always paints DOWN as ok misleads on half of every ML metric … otherwise the colour is decorative, which this document argues against everywhere else.* §086 homes it: *a metric in a row — sparkline — **inside a table cell***, so the polarity declaration belongs on `ColumnDef` beside `role`, which is the same record R-TBL-004's unit wants and needs no adapter change, since an undeclared column is neutral. The tone follows with no choice left open: the arrow's direction is the delta's sign, its tone is `ok` when the movement runs with the declared-good direction and `error` when against, and neutral takes none. **What blocks the build is the arrow itself**: §088 draws `↑` and `↓` and the registry has no glyph record for either — `sort-asc`/`sort-desc` are `▴`/`▾` and scoped `table-header`, a different domain — so the ASCII rung is a visible choice the design does not make. Parked as 26 |
| **R-EVT-001** — Input windows coalesce by identity | `covered` | C16 I45 · C16 I46 · `src/interaction/router/router.ts` `pointerArm` · `test/unit/router-dispatch.test.ts` T1.100, T1.101, T1.101b — **read as what it is, the rule is a constraint on coalescing rather than a requirement to coalesce**, and both things it forbids are impossible by the arm's shape. `pointerArm` is `{id, epoch}` and the commit is `armed.id === id && armed.epoch === ownerEpoch`, so a sample cannot **change the armed target** — a release elsewhere finds a different id — and cannot **replay against a new owner epoch**, because the counter moved and the comparison fails. T1.101b holds the third: a drag cancels the arm and coming back does not restore it. **No pointer-sample coalescing exists** and its absence cannot violate a constraint on it; what matters is that the constraint is enforced at the commit rather than at the sample, so coalescing added later — in the decoder, or inside a window — arrives already bound by it. **Stated blind spot**: this is coverage of the *safety* clause. If a declared sample window is ever wanted for its own sake, §062's is C03's frame window and the input exemption there is a different rule |
| **R-HON-008** — Configuration values name provenance | `owed` | **corrected one pass after it was written: the provenance mechanism is built, and it is the reader that is missing.** `CapabilitySource` (`src/terminal/capabilities.ts`) is `declared | stated | inferred | assumed | unreachable` — the rule's *layer or source* as a five-valued field — and `Answer<T>` is `[value, source]`, **returned together by the rule that reached it**, deliberately, because *a second map from field to source would be the drifting kind: nothing compares a value against a description of where it came from*. All ten capability fields carry one. **And it reaches no reader.** `Detection` is not exported — its own comment says *nothing consumes it yet* — and the only consumers of `.sources` in the tree are `test/unit/capabilities.test.ts` and `test/revert/capabilities.test.ts`. §107 names the site that would take it: *the chosen route appears in `/capabilities`*, and there is no `/capabilities`. So what is owed is the display and the route to it, not the provenance. `TuiConfig` is the separate half: a caller's value meets the framework's constant at a `??` in `src/shell/config.ts`, which keeps neither, and that half has no `Answer` to carry |
| **R-FOC-001** — Unpainted box focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-FOC-002** — Continuous-control focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-FOC-003** — Choice-control focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-FOC-004** — Picture focus | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-FOC-005** — Focus is not row-shaped | `covered` | `src/presentation/blocks/types.ts:447` `level` — an element declares `"block" | "row" | "cell"`, so more than one interactive element per row is expressible by construction and the geometry follows the kind. The §017 census measures the consequence: of 23 kinds in `test/support/design-surfaces.ts` `ONE_PER_KIND`, six publish focusable elements and five draw a treatment, and 17 publish none at all — which is why §017's rungs have no subject in this tree rather than an unbuilt one |
| **R-PTR-001** — Focus-first activation | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PTR-002** — Remembered focus re-resolves | `covered` | C26 I10 · `src/interaction/router/focus.ts` `resolveFocus` · `test/unit/router-focus.test.ts` T1.3h, T1.3i — the store holds an `ElementAddress`, which is `blockId` plus `elementId` and **never an index**, and `resolveFocus` re-resolves it against the live element list on every read, falling back to the block when the element went and to the start when the block went. **The earlier row asked the wrong question.** It looked for a departure hook — a view that leaves and returns — and there is none, because the tree re-resolves on *every* frame rather than on return, which discharges the rule over a strictly larger set of moments. That the one resolver serves both the render read and the next keystroke is the same file's argument (C26 I8): a fall-forward computed on one side only would leave the store holding a dead address |
| **R-PTR-003** — Hover may preview | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PTR-004** — Legend click toggles a series | `covered` | `src/shell/construct.ts:2757` `legendHitAt` — *the toggle itself, shared by the digit key and the legend click* (C16 §4a's legend row, C12 I117), written as **one definition of what a toggle is** so the two cannot disagree. The click reaches the same series state the keyboard does because it reaches the same function |
| **R-PTR-005** — Press arms and release commits | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PTR-006** — Unpaired context click is inert | `covered` | C16 I31 · `src/shell/construct.ts` `button0` · `test/unit/session-mouse.test.ts` T4.67 — **the rule is written in the tree's own words, one line above the gate that holds it.** The gesture table returns `null` for anything that is not `button0`, and its comment says why: *recorded rather than absorbed — a second button has no key equal yet, and a meta- or ctrl-modified click has no ⇧↓-shaped state to reach.* The table **is** the declaration of keyboard equivalence — a click is `↓`, click-again is `⏎`, a drag is `⇧↓` — so *no declared keyboard-equivalent action* and *not in the table* are one predicate, and this rule is R-INT-006's parity read from the other side. **Not vacuous**: the decoder really produces `button2` (`test/unit/router-decode.test.ts` feeds `[<2;10;5M`), and T4.67 asserts both halves — `dispatch` returns false **and** the focus is unchanged, *none of the three is a gesture in the table* |
| **R-REF-001** — No leader sequences | `covered` | `src/interaction/router/types.ts` `Binding` — a binding's `key` is **one** chord record, so a leader or a multi-key sequence is not representable in the type. The rule is held by the shape rather than by a check, which is the strongest form coverage takes: there is no state in which it could be violated and no gate that could go quiet |
| **R-REF-002** — Platform chords remain owned | `covered` | `src/interaction/router/keymap.ts:682` — `agentPrevious` and `agentNext` take `⌥,` and `⌥.`, which replace `⌥⇥`/`⇧⌥⇥`, *which the OS window switcher takes on Windows and most Linux desktops — the compositor never hands them over*. The rule is followed by a binding that was **changed** rather than by a check, and the reason is on the line so a later edit cannot recover the chord by accident (which is R-KEY-006's clause, met here) |
| **R-REF-003** — Bindings do not change availability | `covered` | C16 I35 · `test/unit/router-keymap.test.ts` T1.35 — *a profile is a condition — one table, and `resolve` refuses the wrong one*. `enhanced-terminal` resolves only where `capabilities.keyboardProtocol === "kitty"`, which is a **capability the terminal reports**, not a state that comes and goes: a chord is either exposed or not for the whole session, so there is no indistinguishable visible state for it to work intermittently under |
| **R-TBL-001** — Table alignment follows value kind | `cited` | **resolved to a scope boundary, and parked as 24** — which is what moves this row out of the remainder: the rule is named in `PARKED_QUESTIONS.md` now, and `cited` is not `satisfied`. The mechanism is `src/presentation/table/cells.ts` `decimalPoints`, which derives a column's point *from the column's own cells and never authored* — the exact shape this rule wants, one level up. The obstacle is where the violation lives: `src/data/adapters/fallback.ts` `columnsFor` sets `align: "left"` for every field while already holding the rows, so a numeric column from a far side's JSON is left-aligned. That file is **C07**, which `AUTHORITY.md` names among the components the kit leaves untouched. Deriving the default in C11 instead would override an adapter's declaration, which reaches into C07's authority by another route |
| **R-TBL-002** — Decimal alignment | `covered` | C11 I26 · `src/presentation/table/cells.ts:106` `decimalPoints` — `align: "decimal"` is a column kind of its own and the point's cell is computed per column. **The plan's reconciliation table says this is unbuilt and that is now false** |
| **R-TBL-003** — Missing numeric value is unknown | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-TBL-004** — Numeric grouping and units | `owed` | **both clauses unbuilt, the separator is settled, and the missing piece is one field that three rules want.** *Grouping*: no separator is inserted anywhere in `src/presentation/table/cells.ts`'s cell path, and the design leaves no choice — it draws **`1,204`, `41,208`, `10,000`, `1,136`** across §007, §011, §020 and §021, a comma every time. *The unit*: `ColumnDef` has `key`, `label`, `align`, `priority`, `minWidth`, `maxWidth`, `flex`, `sortable` and `role`, and none is a unit. **`role` is a near-homonym and worth naming so it is not mistaken for the slot**: it is `role?: "expand"`, one value, a structural marker for the renderer-supplied expand column — not a declaration of what kind of value a column holds. **And that declaration is what three rules are waiting on**: grouping needs to know a column holds a quantity rather than a year or an id; R-TBL-001's alignment needs the same fact (parked as 24); R-COL-006's polarity hangs off it (parked as 26 for its arrow). One field, three rules — which is a different shape of work from three unbuilt features |
| **R-TBL-005** — Column-local tail truncation | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-TBL-006** — Sorting belongs to the header | `covered` | `src/presentation/table/cells.ts:191` — the header row carries `sortAsc`/`sortDesc` on the active column, so the sort state is drawn on the header and nowhere else |
| **R-KEY-003** — Conventional key vocabulary | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-KEY-004** — Bindings remain discoverable | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-PRI-002** — Primitive definitions stay locatable | `covered` | `docs/design/language/build-calcium.mjs:415` — `if (!primitive.ruleIds.includes('R-PRI-002')) throw`, plus the check above it that every `definedIn` key names a section that exists. A primitive pointing at a missing canonical section fails the build, so *locatable* is a property of the commit rather than of a reader's diligence |
| **R-PRG-002** — Bar alphabets declare placement | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-MOT-012** — Ramp meaning precedes appearance | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-KEY-005** — Help is a durable generated entry | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-KEY-006** — Key choices retain their rationale | `covered` | `docs/KEYS.md` — generated from `defaultKeymap`, with `test/unit/keymap-table.test.ts` failing when the file and the live keymap disagree, so the record cannot drift from the bindings. It carries both halves the rule asks for: the **convention** (*every action with an `enhanced-terminal` route also has a `both` route, because ⌘, ⇧⏎, ⌃⇧-letters and ⌃⇥ are byte-identical to their unmodified forms without the protocol*) and the **departure** (*every `⌥` route needs the terminal to send Option as Meta*), each with its reason and its history |
| **R-GLY-002** — Reservations are verified in the composed grid | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-KEY-007** — Generated action-map surfaces | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-REG-002** — Normative authority is explicit | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-001** — Two selections, and the mouse belongs to the emulator | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-002** — The render must survive a naive drag | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-003** — A block is atomic in a selection | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-004** — A block's copy form, per kind | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-005** — esc clears the selection; a second esc leaves the rung | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-006** — Selection owns the ground, focus keeps its mark | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-007** — Rectangular copies cells, and says so | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-008** — a, A, and no whole-record key | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-009** — Three things redraw while frozen | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-010** — Buffered arrivals are announced on exit | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-011** — No clipboard is stated, never silent | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-012** — A drag scrolls the container the anchor is in | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-013** — Three autoscroll bands, and it stops at the container's end | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-014** — A container you pass through is taken whole and does not scroll | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-SEL-015** — All-or-none applies continuously | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-THM-002** — A high-contrast theme keeps 7 : 1, and says so | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-THM-003** — A high-contrast theme takes focus and selection as bands | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-THM-004** — A floor's scope is every ground the theme paints text on | `cited` | the R-ID is named outside `docs/design/language/` |
| **R-STA-004** — Availability ranks above a semantic extent | `cited` | the R-ID is named outside `docs/design/language/` |

---

**No total is stated here as prose.** The four figures above are the tool's
output and go stale the moment a row moves; to count them is to run it:

    node tools/rule-status.mjs

