/**
 * Spawning, process groups, streams, handoff.
 *
 * C21 — see spec. The interface is in `types.ts`, which landed with C06; this
 * file holds the behaviour, and `COMPONENT_SOURCES.C21` points here for exactly
 * that reason. A deferral naming C21 is waiting on this file, not on the types.
 *
 * Implement to the spec's commitments and invariants; cite invariant
 * numbers in tests. If the spec is wrong, change the spec first.
 */

export {};
